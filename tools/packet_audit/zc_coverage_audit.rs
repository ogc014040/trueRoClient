// AUTO-GENERATED by lib/network/tools/gen_zc_coverage_audit.py (Step 0 of the
// receive-side packet-version remediation plan). Regenerate when handler.rs
// gains/loses a ZC packet; the test `zc_coverage_audit_drift` fails if this file
// drifts from handler.rs.
//
// Runnable:
//   cargo test -p ragnarok-network zc_coverage_audit -- --nocapture --ignored
//
// It builds the effective server-side ZC id->length table by emulating the C
// preprocessor (MAIN classic build) over three sources -- rathena `packets.hpp`
// (struct sizeof + DEFINE_PACKET_HEADER), Hercules `packets_struct.h`, and legacy
// rathena `clif_packetdb.hpp` -- then compares it against our fully-serialized
// packet length (new + fill_raw_with_packetver) at each target packetver.
#![cfg(test)]
#![allow(clippy::all)]

use packets::packets::*;
use std::collections::BTreeMap;
use std::path::PathBuf;

const TARGETS: &[u32] = &[20120307, 20101124, 20080827, 20070212, 20050719, 20040705];

const CONSUMED_ZC: &[&str] = &[
    "PacketZcAcceptEnter",
    "PacketZcAcceptEnter2",
    "PacketZcAckAddExchangeItem",
    "PacketZcAckBanGuild",
    "PacketZcAckBanGuildSso",
    "PacketZcAckChangeGuildPositioninfo",
    "PacketZcAckCreateChatroom",
    "PacketZcAckDisorganizeGuildResult",
    "PacketZcAckExchangeItem",
    "PacketZcAckExchangeItem2",
    "PacketZcAckGuildMenuinterface",
    "PacketZcAckItemcomposition",
    "PacketZcAckItemidentify",
    "PacketZcAckItemrepair",
    "PacketZcAckLeaveGuild",
    "PacketZcAckMailAddItem",
    "PacketZcAckMailDelete",
    "PacketZcAckMailReturn",
    "PacketZcAckMakeGroup",
    "PacketZcAckOpenstore2",
    "PacketZcAckReqAllyGuild",
    "PacketZcAckReqChangeMembers",
    "PacketZcAckReqDisconnect",
    "PacketZcAckReqHostileGuild",
    "PacketZcAckReqJoinGroup",
    "PacketZcAckReqJoinGuild",
    "PacketZcAckReqmakingitem",
    "PacketZcAckReqname",
    "PacketZcAckReqnameBygid",
    "PacketZcAckReqnameall",
    "PacketZcAckTouseskill",
    "PacketZcAckWeaponrefine",
    "PacketZcAckWhisper",
    "PacketZcActionFailure",
    "PacketZcActiveQuest",
    "PacketZcAddExchangeItem",
    "PacketZcAddExchangeItem2",
    "PacketZcAddFriendsList",
    "PacketZcAddItemToCart",
    "PacketZcAddItemToCart2",
    "PacketZcAddItemToStore",
    "PacketZcAddItemToStore2",
    "PacketZcAddMemberToGroup",
    "PacketZcAddMemberToGroup2",
    "PacketZcAddQuest",
    "PacketZcAddRelatedGuild",
    "PacketZcAddSkill",
    "PacketZcAid",
    "PacketZcAlchemistRank",
    "PacketZcAllQuestList",
    "PacketZcAllQuestMission",
    "PacketZcAttackFailureForDistance",
    "PacketZcAttackRange",
    "PacketZcAutorunSkill",
    "PacketZcAutospelllist",
    "PacketZcBabymsg",
    "PacketZcBanList",
    "PacketZcBlacksmithRank",
    "PacketZcBladestop",
    "PacketZcBroadcast",
    "PacketZcBroadcast2",
    "PacketZcCancelExchangeItem",
    "PacketZcCartEquipmentItemlist",
    "PacketZcCartEquipmentItemlist2",
    "PacketZcCartEquipmentItemlist3",
    "PacketZcCartNormalItemlist",
    "PacketZcCartNormalItemlist2",
    "PacketZcCartNormalItemlist3",
    "PacketZcCartoff",
    "PacketZcChangeChatroom",
    "PacketZcChangeDirection",
    "PacketZcChangeGuild",
    "PacketZcChangestateMer",
    "PacketZcChangestatePet",
    "PacketZcCloseDialog",
    "PacketZcCloseStore",
    "PacketZcConcludeExchangeItem",
    "PacketZcConfig",
    "PacketZcConfigNotify",
    "PacketZcCongratulation",
    "PacketZcCouplename",
    "PacketZcDelQuest",
    "PacketZcDeleteFriends",
    "PacketZcDeleteItemFromBody",
    "PacketZcDeleteItemFromCart",
    "PacketZcDeleteItemFromStore",
    "PacketZcDeleteMemberFromGroup",
    "PacketZcDeleteRelatedGuild",
    "PacketZcDeleteitemFromMcstore",
    "PacketZcDestroyRoom",
    "PacketZcDisappearEntry",
    "PacketZcDispel",
    "PacketZcDivorce",
    "PacketZcEmotion",
    "PacketZcEnterRoom",
    "PacketZcEquipArrow",
    "PacketZcEquipmentItemlist",
    "PacketZcEquipmentItemlist2",
    "PacketZcEquipmentItemlist3",
    "PacketZcExchangeitemUndo",
    "PacketZcExecExchangeItem",
    "PacketZcFastmove",
    "PacketZcFeedMer",
    "PacketZcFeedPet",
    "PacketZcFriendsList",
    "PacketZcFriendsState",
    "PacketZcGroupList",
    "PacketZcGroupinfoChange",
    "PacketZcGuildChat",
    "PacketZcGuildEmblemImg",
    "PacketZcGuildInfo",
    "PacketZcGuildInfo2",
    "PacketZcGuildNotice",
    "PacketZcGuildSkillinfo",
    "PacketZcHighjump",
    "PacketZcHoParChange",
    "PacketZcHoskillinfoList",
    "PacketZcHoskillinfoUpdate",
    "PacketZcItemDisappear",
    "PacketZcItemEntry",
    "PacketZcItemFallEntry",
    "PacketZcItemPickupAck",
    "PacketZcItemPickupAck2",
    "PacketZcItemPickupAck3",
    "PacketZcItemThrowAck",
    "PacketZcItemcompositionList",
    "PacketZcItemidentifyList",
    "PacketZcLoadConfirm",
    "PacketZcLongparChange",
    "PacketZcMailReceive",
    "PacketZcMailReqGetItem",
    "PacketZcMailReqGetList",
    "PacketZcMailReqOpen",
    "PacketZcMailReqSend",
    "PacketZcMailWindows",
    "PacketZcMakableitemlist",
    "PacketZcMakingarrowList",
    "PacketZcMemberExit",
    "PacketZcMemberNewentry",
    "PacketZcMembermgrInfo",
    "PacketZcMenuList",
    "PacketZcMerInit",
    "PacketZcMerParChange",
    "PacketZcMerProperty",
    "PacketZcMerSkillinfoList",
    "PacketZcMerSkillinfoUpdate",
    "PacketZcMissionHunt",
    "PacketZcMsgStateChange",
    "PacketZcMsgStateChange2",
    "PacketZcMvp",
    "PacketZcMyguildBasicInfo",
    "PacketZcNormalItemlist",
    "PacketZcNormalItemlist2",
    "PacketZcNormalItemlist3",
    "PacketZcNotifyAct",
    "PacketZcNotifyAct2",
    "PacketZcNotifyAct3",
    "PacketZcNotifyCartitemCountinfo",
    "PacketZcNotifyChat",
    "PacketZcNotifyChatParty",
    "PacketZcNotifyEffect",
    "PacketZcNotifyEffect2",
    "PacketZcNotifyEffect3",
    "PacketZcNotifyExp",
    "PacketZcNotifyGroundskill",
    "PacketZcNotifyHpToGroupm",
    "PacketZcNotifyHpToGroupmR2",
    "PacketZcNotifyMapproperty",
    "PacketZcNotifyMapproperty2",
    "PacketZcNotifyMove",
    "PacketZcNotifyMoveentry",
    "PacketZcNotifyMoveentry2",
    "PacketZcNotifyMoveentry3",
    "PacketZcNotifyMoveentry4",
    "PacketZcNotifyMoveentry7",
    "PacketZcNotifyMoveentry8",
    "PacketZcNotifyMoveentry9",
    "PacketZcNotifyNewentry",
    "PacketZcNotifyNewentry2",
    "PacketZcNotifyNewentry3",
    "PacketZcNotifyNewentry4",
    "PacketZcNotifyNewentry5",
    "PacketZcNotifyNewentry7",
    "PacketZcNotifyPlayerchat",
    "PacketZcNotifyPlayermove",
    "PacketZcNotifyPositionToGroupm",
    "PacketZcNotifyPositionToGuildm",
    "PacketZcNotifySkill",
    "PacketZcNotifySkill2",
    "PacketZcNotifyStandentry",
    "PacketZcNotifyStandentry2",
    "PacketZcNotifyStandentry3",
    "PacketZcNotifyStandentry4",
    "PacketZcNotifyStandentry5",
    "PacketZcNotifyStandentry6",
    "PacketZcNotifyStandentry7",
    "PacketZcNotifyStoreitemCountinfo",
    "PacketZcNotifyTime",
    "PacketZcNotifyVanish",
    "PacketZcNotifyWeaponitemlist",
    "PacketZcNpcackMapmove",
    "PacketZcNpcspriteChange",
    "PacketZcOpenEditdlg",
    "PacketZcOpenEditdlgstr",
    "PacketZcOpenstore",
    "PacketZcOtherGuildList",
    "PacketZcParChange",
    "PacketZcPartyConfig",
    "PacketZcPartyJoinReq",
    "PacketZcPartyJoinReqAck",
    "PacketZcPcPurchaseItemlist",
    "PacketZcPcPurchaseItemlistFrommc2",
    "PacketZcPcPurchaseMyitemlist",
    "PacketZcPcPurchaseResult",
    "PacketZcPcPurchaseResultFrommc",
    "PacketZcPcSellItemlist",
    "PacketZcPcSellResult",
    "PacketZcPetAct",
    "PacketZcPeteggList",
    "PacketZcPositionIdNameInfo",
    "PacketZcPositionInfo",
    "PacketZcProgressCancel",
    "PacketZcPropertyHomun",
    "PacketZcPropertyPet",
    "PacketZcQuestInfo",
    "PacketZcQuestMissionInfo",
    "PacketZcQuestNotifyEffect",
    "PacketZcRecovery",
    "PacketZcRefuseEnterRoom",
    "PacketZcRepairitemlist",
    "PacketZcReqAddFriends",
    "PacketZcReqAllyGuild",
    "PacketZcReqBaby",
    "PacketZcReqCouple",
    "PacketZcReqExchangeItem",
    "PacketZcReqExchangeItem2",
    "PacketZcReqGroupinfoChangeV2",
    "PacketZcReqJoinGroup",
    "PacketZcReqJoinGuild",
    "PacketZcReqStorePassword",
    "PacketZcReqTakeoffEquipAck",
    "PacketZcReqTakeoffEquipAck2",
    "PacketZcReqWearEquipAck",
    "PacketZcReqWearEquipAck2",
    "PacketZcRestartAck",
    "PacketZcResultMakeGuild",
    "PacketZcResultStorePassword",
    "PacketZcResurrection",
    "PacketZcRoleChange",
    "PacketZcRoomNewentry",
    "PacketZcSayDialog",
    "PacketZcSelectDealtype",
    "PacketZcShortcutKeyList",
    "PacketZcShortcutKeyListV2",
    "PacketZcShowImage2",
    "PacketZcSkillDisappear",
    "PacketZcSkillEntry",
    "PacketZcSkillPostdelay",
    "PacketZcSkillUpdate",
    "PacketZcSkillinfoList",
    "PacketZcSkillinfoUpdate",
    "PacketZcSkillinfoUpdate2",
    "PacketZcSound",
    "PacketZcSpirits",
    "PacketZcSpirits2",
    "PacketZcSpriteChange",
    "PacketZcSpriteChange2",
    "PacketZcStartCapture",
    "PacketZcStartCouple",
    "PacketZcStateChange3",
    "PacketZcStatus",
    "PacketZcStatusValues",
    "PacketZcStopmove",
    "PacketZcStoreEntry",
    "PacketZcStoreEquipmentItemlist",
    "PacketZcStoreEquipmentItemlist2",
    "PacketZcStoreEquipmentItemlist3",
    "PacketZcStoreNormalItemlist",
    "PacketZcStoreNormalItemlist2",
    "PacketZcStoreNormalItemlist3",
    "PacketZcTaekwonRank",
    "PacketZcTrycaptureMonster",
    "PacketZcUpdateGdid",
    "PacketZcUpdateMissionHunt",
    "PacketZcUseItemAck",
    "PacketZcUseItemAck2",
    "PacketZcUseSkill",
    "PacketZcUserCount",
    "PacketZcUseskillAck",
    "PacketZcUseskillAck2",
    "PacketZcWaitDialog",
    "PacketZcWarplist",
    "PacketZcWhisper",
];

fn manifest_dir() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
}

// "0xLLHH" wire-byte-concat -> canonical little-endian u16 id
fn canon_id(s: &str) -> u16 {
    let h = s.trim_start_matches("0x").trim_start_matches("0X");
    let b0 = u8::from_str_radix(&h[0..2], 16).unwrap();
    let b1 = u8::from_str_radix(&h[2..4], 16).unwrap();
    (b0 as u16) | ((b1 as u16) << 8)
}

// name -> (id-at-ver, filled-wire-len-at-ver). Length is the fully-serialized size
// (new + fill_raw_with_packetver) so nested fixed sub-structs are counted -- unlike
// base_len(), which returns header-only for such structs. id is None for
// nested/builder-only structs with no PacketId.
fn zc_probe(name: &str, v: u32) -> Option<(Option<u16>, usize)> {
    Some(match name {
        "PacketZcAcceptEnter" => (Some(canon_id(PacketZcAcceptEnter::packet_id(v))), { let mut p = PacketZcAcceptEnter::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAcceptEnter::base_len(v)) }),
        "PacketZcAcceptEnter2" => (Some(canon_id(PacketZcAcceptEnter2::packet_id(v))), { let mut p = PacketZcAcceptEnter2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAcceptEnter2::base_len(v)) }),
        "PacketZcAckAddExchangeItem" => (Some(canon_id(PacketZcAckAddExchangeItem::packet_id(v))), { let mut p = PacketZcAckAddExchangeItem::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckAddExchangeItem::base_len(v)) }),
        "PacketZcAckBanGuild" => (Some(canon_id(PacketZcAckBanGuild::packet_id(v))), { let mut p = PacketZcAckBanGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckBanGuild::base_len(v)) }),
        "PacketZcAckBanGuildSso" => (Some(canon_id(PacketZcAckBanGuildSso::packet_id(v))), { let mut p = PacketZcAckBanGuildSso::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckBanGuildSso::base_len(v)) }),
        "PacketZcAckChangeGuildPositioninfo" => (Some(canon_id(PacketZcAckChangeGuildPositioninfo::packet_id(v))), { let mut p = PacketZcAckChangeGuildPositioninfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckChangeGuildPositioninfo::base_len(v)) }),
        "PacketZcAckCreateChatroom" => (Some(canon_id(PacketZcAckCreateChatroom::packet_id(v))), { let mut p = PacketZcAckCreateChatroom::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckCreateChatroom::base_len(v)) }),
        "PacketZcAckDisorganizeGuildResult" => (Some(canon_id(PacketZcAckDisorganizeGuildResult::packet_id(v))), { let mut p = PacketZcAckDisorganizeGuildResult::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckDisorganizeGuildResult::base_len(v)) }),
        "PacketZcAckExchangeItem" => (Some(canon_id(PacketZcAckExchangeItem::packet_id(v))), { let mut p = PacketZcAckExchangeItem::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckExchangeItem::base_len(v)) }),
        "PacketZcAckExchangeItem2" => (Some(canon_id(PacketZcAckExchangeItem2::packet_id(v))), { let mut p = PacketZcAckExchangeItem2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckExchangeItem2::base_len(v)) }),
        "PacketZcAckGuildMenuinterface" => (Some(canon_id(PacketZcAckGuildMenuinterface::packet_id(v))), { let mut p = PacketZcAckGuildMenuinterface::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckGuildMenuinterface::base_len(v)) }),
        "PacketZcAckItemcomposition" => (Some(canon_id(PacketZcAckItemcomposition::packet_id(v))), { let mut p = PacketZcAckItemcomposition::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckItemcomposition::base_len(v)) }),
        "PacketZcAckItemidentify" => (Some(canon_id(PacketZcAckItemidentify::packet_id(v))), { let mut p = PacketZcAckItemidentify::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckItemidentify::base_len(v)) }),
        "PacketZcAckItemrepair" => (Some(canon_id(PacketZcAckItemrepair::packet_id(v))), { let mut p = PacketZcAckItemrepair::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckItemrepair::base_len(v)) }),
        "PacketZcAckLeaveGuild" => (Some(canon_id(PacketZcAckLeaveGuild::packet_id(v))), { let mut p = PacketZcAckLeaveGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckLeaveGuild::base_len(v)) }),
        "PacketZcAckMailAddItem" => (Some(canon_id(PacketZcAckMailAddItem::packet_id(v))), { let mut p = PacketZcAckMailAddItem::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckMailAddItem::base_len(v)) }),
        "PacketZcAckMailDelete" => (Some(canon_id(PacketZcAckMailDelete::packet_id(v))), { let mut p = PacketZcAckMailDelete::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckMailDelete::base_len(v)) }),
        "PacketZcAckMailReturn" => (Some(canon_id(PacketZcAckMailReturn::packet_id(v))), { let mut p = PacketZcAckMailReturn::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckMailReturn::base_len(v)) }),
        "PacketZcAckMakeGroup" => (Some(canon_id(PacketZcAckMakeGroup::packet_id(v))), { let mut p = PacketZcAckMakeGroup::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckMakeGroup::base_len(v)) }),
        "PacketZcAckOpenstore2" => (Some(canon_id(PacketZcAckOpenstore2::packet_id(v))), { let mut p = PacketZcAckOpenstore2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckOpenstore2::base_len(v)) }),
        "PacketZcAckReqAllyGuild" => (Some(canon_id(PacketZcAckReqAllyGuild::packet_id(v))), { let mut p = PacketZcAckReqAllyGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckReqAllyGuild::base_len(v)) }),
        "PacketZcAckReqChangeMembers" => (Some(canon_id(PacketZcAckReqChangeMembers::packet_id(v))), { let mut p = PacketZcAckReqChangeMembers::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckReqChangeMembers::base_len(v)) }),
        "PacketZcAckReqDisconnect" => (Some(canon_id(PacketZcAckReqDisconnect::packet_id(v))), { let mut p = PacketZcAckReqDisconnect::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckReqDisconnect::base_len(v)) }),
        "PacketZcAckReqHostileGuild" => (Some(canon_id(PacketZcAckReqHostileGuild::packet_id(v))), { let mut p = PacketZcAckReqHostileGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckReqHostileGuild::base_len(v)) }),
        "PacketZcAckReqJoinGroup" => (Some(canon_id(PacketZcAckReqJoinGroup::packet_id(v))), { let mut p = PacketZcAckReqJoinGroup::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckReqJoinGroup::base_len(v)) }),
        "PacketZcAckReqJoinGuild" => (Some(canon_id(PacketZcAckReqJoinGuild::packet_id(v))), { let mut p = PacketZcAckReqJoinGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckReqJoinGuild::base_len(v)) }),
        "PacketZcAckReqmakingitem" => (Some(canon_id(PacketZcAckReqmakingitem::packet_id(v))), { let mut p = PacketZcAckReqmakingitem::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckReqmakingitem::base_len(v)) }),
        "PacketZcAckReqname" => (Some(canon_id(PacketZcAckReqname::packet_id(v))), { let mut p = PacketZcAckReqname::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckReqname::base_len(v)) }),
        "PacketZcAckReqnameBygid" => (Some(canon_id(PacketZcAckReqnameBygid::packet_id(v))), { let mut p = PacketZcAckReqnameBygid::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckReqnameBygid::base_len(v)) }),
        "PacketZcAckReqnameall" => (Some(canon_id(PacketZcAckReqnameall::packet_id(v))), { let mut p = PacketZcAckReqnameall::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckReqnameall::base_len(v)) }),
        "PacketZcAckTouseskill" => (Some(canon_id(PacketZcAckTouseskill::packet_id(v))), { let mut p = PacketZcAckTouseskill::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckTouseskill::base_len(v)) }),
        "PacketZcAckWeaponrefine" => (Some(canon_id(PacketZcAckWeaponrefine::packet_id(v))), { let mut p = PacketZcAckWeaponrefine::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckWeaponrefine::base_len(v)) }),
        "PacketZcAckWhisper" => (Some(canon_id(PacketZcAckWhisper::packet_id(v))), { let mut p = PacketZcAckWhisper::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAckWhisper::base_len(v)) }),
        "PacketZcActionFailure" => (Some(canon_id(PacketZcActionFailure::packet_id(v))), { let mut p = PacketZcActionFailure::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcActionFailure::base_len(v)) }),
        "PacketZcActiveQuest" => (Some(canon_id(PacketZcActiveQuest::packet_id(v))), { let mut p = PacketZcActiveQuest::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcActiveQuest::base_len(v)) }),
        "PacketZcAddExchangeItem" => (Some(canon_id(PacketZcAddExchangeItem::packet_id(v))), { let mut p = PacketZcAddExchangeItem::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddExchangeItem::base_len(v)) }),
        "PacketZcAddExchangeItem2" => (Some(canon_id(PacketZcAddExchangeItem2::packet_id(v))), { let mut p = PacketZcAddExchangeItem2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddExchangeItem2::base_len(v)) }),
        "PacketZcAddFriendsList" => (Some(canon_id(PacketZcAddFriendsList::packet_id(v))), { let mut p = PacketZcAddFriendsList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddFriendsList::base_len(v)) }),
        "PacketZcAddItemToCart" => (Some(canon_id(PacketZcAddItemToCart::packet_id(v))), { let mut p = PacketZcAddItemToCart::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddItemToCart::base_len(v)) }),
        "PacketZcAddItemToCart2" => (Some(canon_id(PacketZcAddItemToCart2::packet_id(v))), { let mut p = PacketZcAddItemToCart2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddItemToCart2::base_len(v)) }),
        "PacketZcAddItemToStore" => (Some(canon_id(PacketZcAddItemToStore::packet_id(v))), { let mut p = PacketZcAddItemToStore::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddItemToStore::base_len(v)) }),
        "PacketZcAddItemToStore2" => (Some(canon_id(PacketZcAddItemToStore2::packet_id(v))), { let mut p = PacketZcAddItemToStore2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddItemToStore2::base_len(v)) }),
        "PacketZcAddMemberToGroup" => (Some(canon_id(PacketZcAddMemberToGroup::packet_id(v))), { let mut p = PacketZcAddMemberToGroup::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddMemberToGroup::base_len(v)) }),
        "PacketZcAddMemberToGroup2" => (Some(canon_id(PacketZcAddMemberToGroup2::packet_id(v))), { let mut p = PacketZcAddMemberToGroup2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddMemberToGroup2::base_len(v)) }),
        "PacketZcAddQuest" => (Some(canon_id(PacketZcAddQuest::packet_id(v))), { let mut p = PacketZcAddQuest::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddQuest::base_len(v)) }),
        "PacketZcAddRelatedGuild" => (Some(canon_id(PacketZcAddRelatedGuild::packet_id(v))), { let mut p = PacketZcAddRelatedGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddRelatedGuild::base_len(v)) }),
        "PacketZcAddSkill" => (Some(canon_id(PacketZcAddSkill::packet_id(v))), { let mut p = PacketZcAddSkill::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAddSkill::base_len(v)) }),
        "PacketZcAid" => (Some(canon_id(PacketZcAid::packet_id(v))), { let mut p = PacketZcAid::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAid::base_len(v)) }),
        "PacketZcAlchemistRank" => (Some(canon_id(PacketZcAlchemistRank::packet_id(v))), { let mut p = PacketZcAlchemistRank::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAlchemistRank::base_len(v)) }),
        "PacketZcAllQuestList" => (Some(canon_id(PacketZcAllQuestList::packet_id(v))), { let mut p = PacketZcAllQuestList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAllQuestList::base_len(v)) }),
        "PacketZcAllQuestMission" => (Some(canon_id(PacketZcAllQuestMission::packet_id(v))), { let mut p = PacketZcAllQuestMission::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAllQuestMission::base_len(v)) }),
        "PacketZcAttackFailureForDistance" => (Some(canon_id(PacketZcAttackFailureForDistance::packet_id(v))), { let mut p = PacketZcAttackFailureForDistance::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAttackFailureForDistance::base_len(v)) }),
        "PacketZcAttackRange" => (Some(canon_id(PacketZcAttackRange::packet_id(v))), { let mut p = PacketZcAttackRange::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAttackRange::base_len(v)) }),
        "PacketZcAutorunSkill" => (Some(canon_id(PacketZcAutorunSkill::packet_id(v))), { let mut p = PacketZcAutorunSkill::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAutorunSkill::base_len(v)) }),
        "PacketZcAutospelllist" => (Some(canon_id(PacketZcAutospelllist::packet_id(v))), { let mut p = PacketZcAutospelllist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcAutospelllist::base_len(v)) }),
        "PacketZcBabymsg" => (Some(canon_id(PacketZcBabymsg::packet_id(v))), { let mut p = PacketZcBabymsg::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcBabymsg::base_len(v)) }),
        "PacketZcBanList" => (Some(canon_id(PacketZcBanList::packet_id(v))), { let mut p = PacketZcBanList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcBanList::base_len(v)) }),
        "PacketZcBlacksmithRank" => (Some(canon_id(PacketZcBlacksmithRank::packet_id(v))), { let mut p = PacketZcBlacksmithRank::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcBlacksmithRank::base_len(v)) }),
        "PacketZcBladestop" => (Some(canon_id(PacketZcBladestop::packet_id(v))), { let mut p = PacketZcBladestop::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcBladestop::base_len(v)) }),
        "PacketZcBroadcast" => (Some(canon_id(PacketZcBroadcast::packet_id(v))), { let mut p = PacketZcBroadcast::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcBroadcast::base_len(v)) }),
        "PacketZcBroadcast2" => (Some(canon_id(PacketZcBroadcast2::packet_id(v))), { let mut p = PacketZcBroadcast2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcBroadcast2::base_len(v)) }),
        "PacketZcCancelExchangeItem" => (Some(canon_id(PacketZcCancelExchangeItem::packet_id(v))), { let mut p = PacketZcCancelExchangeItem::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCancelExchangeItem::base_len(v)) }),
        "PacketZcCartEquipmentItemlist" => (Some(canon_id(PacketZcCartEquipmentItemlist::packet_id(v))), { let mut p = PacketZcCartEquipmentItemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCartEquipmentItemlist::base_len(v)) }),
        "PacketZcCartEquipmentItemlist2" => (Some(canon_id(PacketZcCartEquipmentItemlist2::packet_id(v))), { let mut p = PacketZcCartEquipmentItemlist2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCartEquipmentItemlist2::base_len(v)) }),
        "PacketZcCartEquipmentItemlist3" => (Some(canon_id(PacketZcCartEquipmentItemlist3::packet_id(v))), { let mut p = PacketZcCartEquipmentItemlist3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCartEquipmentItemlist3::base_len(v)) }),
        "PacketZcCartNormalItemlist" => (Some(canon_id(PacketZcCartNormalItemlist::packet_id(v))), { let mut p = PacketZcCartNormalItemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCartNormalItemlist::base_len(v)) }),
        "PacketZcCartNormalItemlist2" => (Some(canon_id(PacketZcCartNormalItemlist2::packet_id(v))), { let mut p = PacketZcCartNormalItemlist2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCartNormalItemlist2::base_len(v)) }),
        "PacketZcCartNormalItemlist3" => (Some(canon_id(PacketZcCartNormalItemlist3::packet_id(v))), { let mut p = PacketZcCartNormalItemlist3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCartNormalItemlist3::base_len(v)) }),
        "PacketZcCartoff" => (Some(canon_id(PacketZcCartoff::packet_id(v))), { let mut p = PacketZcCartoff::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCartoff::base_len(v)) }),
        "PacketZcChangeChatroom" => (Some(canon_id(PacketZcChangeChatroom::packet_id(v))), { let mut p = PacketZcChangeChatroom::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcChangeChatroom::base_len(v)) }),
        "PacketZcChangeDirection" => (Some(canon_id(PacketZcChangeDirection::packet_id(v))), { let mut p = PacketZcChangeDirection::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcChangeDirection::base_len(v)) }),
        "PacketZcChangeGuild" => (Some(canon_id(PacketZcChangeGuild::packet_id(v))), { let mut p = PacketZcChangeGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcChangeGuild::base_len(v)) }),
        "PacketZcChangestateMer" => (Some(canon_id(PacketZcChangestateMer::packet_id(v))), { let mut p = PacketZcChangestateMer::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcChangestateMer::base_len(v)) }),
        "PacketZcChangestatePet" => (Some(canon_id(PacketZcChangestatePet::packet_id(v))), { let mut p = PacketZcChangestatePet::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcChangestatePet::base_len(v)) }),
        "PacketZcCloseDialog" => (Some(canon_id(PacketZcCloseDialog::packet_id(v))), { let mut p = PacketZcCloseDialog::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCloseDialog::base_len(v)) }),
        "PacketZcCloseStore" => (Some(canon_id(PacketZcCloseStore::packet_id(v))), { let mut p = PacketZcCloseStore::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCloseStore::base_len(v)) }),
        "PacketZcConcludeExchangeItem" => (Some(canon_id(PacketZcConcludeExchangeItem::packet_id(v))), { let mut p = PacketZcConcludeExchangeItem::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcConcludeExchangeItem::base_len(v)) }),
        "PacketZcConfig" => (Some(canon_id(PacketZcConfig::packet_id(v))), { let mut p = PacketZcConfig::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcConfig::base_len(v)) }),
        "PacketZcConfigNotify" => (Some(canon_id(PacketZcConfigNotify::packet_id(v))), { let mut p = PacketZcConfigNotify::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcConfigNotify::base_len(v)) }),
        "PacketZcCongratulation" => (Some(canon_id(PacketZcCongratulation::packet_id(v))), { let mut p = PacketZcCongratulation::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCongratulation::base_len(v)) }),
        "PacketZcCouplename" => (Some(canon_id(PacketZcCouplename::packet_id(v))), { let mut p = PacketZcCouplename::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcCouplename::base_len(v)) }),
        "PacketZcDelQuest" => (Some(canon_id(PacketZcDelQuest::packet_id(v))), { let mut p = PacketZcDelQuest::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDelQuest::base_len(v)) }),
        "PacketZcDeleteFriends" => (Some(canon_id(PacketZcDeleteFriends::packet_id(v))), { let mut p = PacketZcDeleteFriends::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDeleteFriends::base_len(v)) }),
        "PacketZcDeleteItemFromBody" => (Some(canon_id(PacketZcDeleteItemFromBody::packet_id(v))), { let mut p = PacketZcDeleteItemFromBody::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDeleteItemFromBody::base_len(v)) }),
        "PacketZcDeleteItemFromCart" => (Some(canon_id(PacketZcDeleteItemFromCart::packet_id(v))), { let mut p = PacketZcDeleteItemFromCart::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDeleteItemFromCart::base_len(v)) }),
        "PacketZcDeleteItemFromStore" => (Some(canon_id(PacketZcDeleteItemFromStore::packet_id(v))), { let mut p = PacketZcDeleteItemFromStore::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDeleteItemFromStore::base_len(v)) }),
        "PacketZcDeleteMemberFromGroup" => (Some(canon_id(PacketZcDeleteMemberFromGroup::packet_id(v))), { let mut p = PacketZcDeleteMemberFromGroup::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDeleteMemberFromGroup::base_len(v)) }),
        "PacketZcDeleteRelatedGuild" => (Some(canon_id(PacketZcDeleteRelatedGuild::packet_id(v))), { let mut p = PacketZcDeleteRelatedGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDeleteRelatedGuild::base_len(v)) }),
        "PacketZcDeleteitemFromMcstore" => (Some(canon_id(PacketZcDeleteitemFromMcstore::packet_id(v))), { let mut p = PacketZcDeleteitemFromMcstore::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDeleteitemFromMcstore::base_len(v)) }),
        "PacketZcDestroyRoom" => (Some(canon_id(PacketZcDestroyRoom::packet_id(v))), { let mut p = PacketZcDestroyRoom::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDestroyRoom::base_len(v)) }),
        "PacketZcDisappearEntry" => (Some(canon_id(PacketZcDisappearEntry::packet_id(v))), { let mut p = PacketZcDisappearEntry::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDisappearEntry::base_len(v)) }),
        "PacketZcDispel" => (Some(canon_id(PacketZcDispel::packet_id(v))), { let mut p = PacketZcDispel::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDispel::base_len(v)) }),
        "PacketZcDivorce" => (Some(canon_id(PacketZcDivorce::packet_id(v))), { let mut p = PacketZcDivorce::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcDivorce::base_len(v)) }),
        "PacketZcEmotion" => (Some(canon_id(PacketZcEmotion::packet_id(v))), { let mut p = PacketZcEmotion::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcEmotion::base_len(v)) }),
        "PacketZcEnterRoom" => (Some(canon_id(PacketZcEnterRoom::packet_id(v))), { let mut p = PacketZcEnterRoom::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcEnterRoom::base_len(v)) }),
        "PacketZcEquipArrow" => (Some(canon_id(PacketZcEquipArrow::packet_id(v))), { let mut p = PacketZcEquipArrow::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcEquipArrow::base_len(v)) }),
        "PacketZcEquipmentItemlist" => (Some(canon_id(PacketZcEquipmentItemlist::packet_id(v))), { let mut p = PacketZcEquipmentItemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcEquipmentItemlist::base_len(v)) }),
        "PacketZcEquipmentItemlist2" => (Some(canon_id(PacketZcEquipmentItemlist2::packet_id(v))), { let mut p = PacketZcEquipmentItemlist2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcEquipmentItemlist2::base_len(v)) }),
        "PacketZcEquipmentItemlist3" => (Some(canon_id(PacketZcEquipmentItemlist3::packet_id(v))), { let mut p = PacketZcEquipmentItemlist3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcEquipmentItemlist3::base_len(v)) }),
        "PacketZcExchangeitemUndo" => (Some(canon_id(PacketZcExchangeitemUndo::packet_id(v))), { let mut p = PacketZcExchangeitemUndo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcExchangeitemUndo::base_len(v)) }),
        "PacketZcExecExchangeItem" => (Some(canon_id(PacketZcExecExchangeItem::packet_id(v))), { let mut p = PacketZcExecExchangeItem::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcExecExchangeItem::base_len(v)) }),
        "PacketZcFastmove" => (Some(canon_id(PacketZcFastmove::packet_id(v))), { let mut p = PacketZcFastmove::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcFastmove::base_len(v)) }),
        "PacketZcFeedMer" => (Some(canon_id(PacketZcFeedMer::packet_id(v))), { let mut p = PacketZcFeedMer::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcFeedMer::base_len(v)) }),
        "PacketZcFeedPet" => (Some(canon_id(PacketZcFeedPet::packet_id(v))), { let mut p = PacketZcFeedPet::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcFeedPet::base_len(v)) }),
        "PacketZcFriendsList" => (Some(canon_id(PacketZcFriendsList::packet_id(v))), { let mut p = PacketZcFriendsList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcFriendsList::base_len(v)) }),
        "PacketZcFriendsState" => (Some(canon_id(PacketZcFriendsState::packet_id(v))), { let mut p = PacketZcFriendsState::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcFriendsState::base_len(v)) }),
        "PacketZcGroupList" => (Some(canon_id(PacketZcGroupList::packet_id(v))), { let mut p = PacketZcGroupList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcGroupList::base_len(v)) }),
        "PacketZcGroupinfoChange" => (Some(canon_id(PacketZcGroupinfoChange::packet_id(v))), { let mut p = PacketZcGroupinfoChange::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcGroupinfoChange::base_len(v)) }),
        "PacketZcGuildChat" => (Some(canon_id(PacketZcGuildChat::packet_id(v))), { let mut p = PacketZcGuildChat::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcGuildChat::base_len(v)) }),
        "PacketZcGuildEmblemImg" => (Some(canon_id(PacketZcGuildEmblemImg::packet_id(v))), { let mut p = PacketZcGuildEmblemImg::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcGuildEmblemImg::base_len(v)) }),
        "PacketZcGuildInfo" => (Some(canon_id(PacketZcGuildInfo::packet_id(v))), { let mut p = PacketZcGuildInfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcGuildInfo::base_len(v)) }),
        "PacketZcGuildInfo2" => (Some(canon_id(PacketZcGuildInfo2::packet_id(v))), { let mut p = PacketZcGuildInfo2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcGuildInfo2::base_len(v)) }),
        "PacketZcGuildNotice" => (Some(canon_id(PacketZcGuildNotice::packet_id(v))), { let mut p = PacketZcGuildNotice::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcGuildNotice::base_len(v)) }),
        "PacketZcGuildSkillinfo" => (Some(canon_id(PacketZcGuildSkillinfo::packet_id(v))), { let mut p = PacketZcGuildSkillinfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcGuildSkillinfo::base_len(v)) }),
        "PacketZcHighjump" => (Some(canon_id(PacketZcHighjump::packet_id(v))), { let mut p = PacketZcHighjump::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcHighjump::base_len(v)) }),
        "PacketZcHoParChange" => (Some(canon_id(PacketZcHoParChange::packet_id(v))), { let mut p = PacketZcHoParChange::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcHoParChange::base_len(v)) }),
        "PacketZcHoskillinfoList" => (Some(canon_id(PacketZcHoskillinfoList::packet_id(v))), { let mut p = PacketZcHoskillinfoList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcHoskillinfoList::base_len(v)) }),
        "PacketZcHoskillinfoUpdate" => (Some(canon_id(PacketZcHoskillinfoUpdate::packet_id(v))), { let mut p = PacketZcHoskillinfoUpdate::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcHoskillinfoUpdate::base_len(v)) }),
        "PacketZcItemDisappear" => (Some(canon_id(PacketZcItemDisappear::packet_id(v))), { let mut p = PacketZcItemDisappear::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcItemDisappear::base_len(v)) }),
        "PacketZcItemEntry" => (Some(canon_id(PacketZcItemEntry::packet_id(v))), { let mut p = PacketZcItemEntry::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcItemEntry::base_len(v)) }),
        "PacketZcItemFallEntry" => (Some(canon_id(PacketZcItemFallEntry::packet_id(v))), { let mut p = PacketZcItemFallEntry::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcItemFallEntry::base_len(v)) }),
        "PacketZcItemPickupAck" => (Some(canon_id(PacketZcItemPickupAck::packet_id(v))), { let mut p = PacketZcItemPickupAck::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcItemPickupAck::base_len(v)) }),
        "PacketZcItemPickupAck2" => (Some(canon_id(PacketZcItemPickupAck2::packet_id(v))), { let mut p = PacketZcItemPickupAck2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcItemPickupAck2::base_len(v)) }),
        "PacketZcItemPickupAck3" => (Some(canon_id(PacketZcItemPickupAck3::packet_id(v))), { let mut p = PacketZcItemPickupAck3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcItemPickupAck3::base_len(v)) }),
        "PacketZcItemThrowAck" => (Some(canon_id(PacketZcItemThrowAck::packet_id(v))), { let mut p = PacketZcItemThrowAck::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcItemThrowAck::base_len(v)) }),
        "PacketZcItemcompositionList" => (Some(canon_id(PacketZcItemcompositionList::packet_id(v))), { let mut p = PacketZcItemcompositionList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcItemcompositionList::base_len(v)) }),
        "PacketZcItemidentifyList" => (Some(canon_id(PacketZcItemidentifyList::packet_id(v))), { let mut p = PacketZcItemidentifyList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcItemidentifyList::base_len(v)) }),
        "PacketZcLoadConfirm" => (Some(canon_id(PacketZcLoadConfirm::packet_id(v))), { let mut p = PacketZcLoadConfirm::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcLoadConfirm::base_len(v)) }),
        "PacketZcLongparChange" => (Some(canon_id(PacketZcLongparChange::packet_id(v))), { let mut p = PacketZcLongparChange::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcLongparChange::base_len(v)) }),
        "PacketZcMailReceive" => (Some(canon_id(PacketZcMailReceive::packet_id(v))), { let mut p = PacketZcMailReceive::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMailReceive::base_len(v)) }),
        "PacketZcMailReqGetItem" => (Some(canon_id(PacketZcMailReqGetItem::packet_id(v))), { let mut p = PacketZcMailReqGetItem::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMailReqGetItem::base_len(v)) }),
        "PacketZcMailReqGetList" => (Some(canon_id(PacketZcMailReqGetList::packet_id(v))), { let mut p = PacketZcMailReqGetList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMailReqGetList::base_len(v)) }),
        "PacketZcMailReqOpen" => (Some(canon_id(PacketZcMailReqOpen::packet_id(v))), { let mut p = PacketZcMailReqOpen::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMailReqOpen::base_len(v)) }),
        "PacketZcMailReqSend" => (Some(canon_id(PacketZcMailReqSend::packet_id(v))), { let mut p = PacketZcMailReqSend::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMailReqSend::base_len(v)) }),
        "PacketZcMailWindows" => (Some(canon_id(PacketZcMailWindows::packet_id(v))), { let mut p = PacketZcMailWindows::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMailWindows::base_len(v)) }),
        "PacketZcMakableitemlist" => (Some(canon_id(PacketZcMakableitemlist::packet_id(v))), { let mut p = PacketZcMakableitemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMakableitemlist::base_len(v)) }),
        "PacketZcMakingarrowList" => (Some(canon_id(PacketZcMakingarrowList::packet_id(v))), { let mut p = PacketZcMakingarrowList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMakingarrowList::base_len(v)) }),
        "PacketZcMemberExit" => (Some(canon_id(PacketZcMemberExit::packet_id(v))), { let mut p = PacketZcMemberExit::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMemberExit::base_len(v)) }),
        "PacketZcMemberNewentry" => (Some(canon_id(PacketZcMemberNewentry::packet_id(v))), { let mut p = PacketZcMemberNewentry::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMemberNewentry::base_len(v)) }),
        "PacketZcMembermgrInfo" => (Some(canon_id(PacketZcMembermgrInfo::packet_id(v))), { let mut p = PacketZcMembermgrInfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMembermgrInfo::base_len(v)) }),
        "PacketZcMenuList" => (Some(canon_id(PacketZcMenuList::packet_id(v))), { let mut p = PacketZcMenuList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMenuList::base_len(v)) }),
        "PacketZcMerInit" => (Some(canon_id(PacketZcMerInit::packet_id(v))), { let mut p = PacketZcMerInit::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMerInit::base_len(v)) }),
        "PacketZcMerParChange" => (Some(canon_id(PacketZcMerParChange::packet_id(v))), { let mut p = PacketZcMerParChange::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMerParChange::base_len(v)) }),
        "PacketZcMerProperty" => (Some(canon_id(PacketZcMerProperty::packet_id(v))), { let mut p = PacketZcMerProperty::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMerProperty::base_len(v)) }),
        "PacketZcMerSkillinfoList" => (Some(canon_id(PacketZcMerSkillinfoList::packet_id(v))), { let mut p = PacketZcMerSkillinfoList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMerSkillinfoList::base_len(v)) }),
        "PacketZcMerSkillinfoUpdate" => (Some(canon_id(PacketZcMerSkillinfoUpdate::packet_id(v))), { let mut p = PacketZcMerSkillinfoUpdate::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMerSkillinfoUpdate::base_len(v)) }),
        "PacketZcMissionHunt" => (None, { let mut p = PacketZcMissionHunt::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMissionHunt::base_len(v)) }),
        "PacketZcMsgStateChange" => (Some(canon_id(PacketZcMsgStateChange::packet_id(v))), { let mut p = PacketZcMsgStateChange::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMsgStateChange::base_len(v)) }),
        "PacketZcMsgStateChange2" => (Some(canon_id(PacketZcMsgStateChange2::packet_id(v))), { let mut p = PacketZcMsgStateChange2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMsgStateChange2::base_len(v)) }),
        "PacketZcMvp" => (Some(canon_id(PacketZcMvp::packet_id(v))), { let mut p = PacketZcMvp::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMvp::base_len(v)) }),
        "PacketZcMyguildBasicInfo" => (Some(canon_id(PacketZcMyguildBasicInfo::packet_id(v))), { let mut p = PacketZcMyguildBasicInfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcMyguildBasicInfo::base_len(v)) }),
        "PacketZcNormalItemlist" => (Some(canon_id(PacketZcNormalItemlist::packet_id(v))), { let mut p = PacketZcNormalItemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNormalItemlist::base_len(v)) }),
        "PacketZcNormalItemlist2" => (Some(canon_id(PacketZcNormalItemlist2::packet_id(v))), { let mut p = PacketZcNormalItemlist2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNormalItemlist2::base_len(v)) }),
        "PacketZcNormalItemlist3" => (Some(canon_id(PacketZcNormalItemlist3::packet_id(v))), { let mut p = PacketZcNormalItemlist3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNormalItemlist3::base_len(v)) }),
        "PacketZcNotifyAct" => (Some(canon_id(PacketZcNotifyAct::packet_id(v))), { let mut p = PacketZcNotifyAct::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyAct::base_len(v)) }),
        "PacketZcNotifyAct2" => (Some(canon_id(PacketZcNotifyAct2::packet_id(v))), { let mut p = PacketZcNotifyAct2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyAct2::base_len(v)) }),
        "PacketZcNotifyAct3" => (Some(canon_id(PacketZcNotifyAct3::packet_id(v))), { let mut p = PacketZcNotifyAct3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyAct3::base_len(v)) }),
        "PacketZcNotifyCartitemCountinfo" => (Some(canon_id(PacketZcNotifyCartitemCountinfo::packet_id(v))), { let mut p = PacketZcNotifyCartitemCountinfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyCartitemCountinfo::base_len(v)) }),
        "PacketZcNotifyChat" => (Some(canon_id(PacketZcNotifyChat::packet_id(v))), { let mut p = PacketZcNotifyChat::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyChat::base_len(v)) }),
        "PacketZcNotifyChatParty" => (Some(canon_id(PacketZcNotifyChatParty::packet_id(v))), { let mut p = PacketZcNotifyChatParty::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyChatParty::base_len(v)) }),
        "PacketZcNotifyEffect" => (Some(canon_id(PacketZcNotifyEffect::packet_id(v))), { let mut p = PacketZcNotifyEffect::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyEffect::base_len(v)) }),
        "PacketZcNotifyEffect2" => (Some(canon_id(PacketZcNotifyEffect2::packet_id(v))), { let mut p = PacketZcNotifyEffect2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyEffect2::base_len(v)) }),
        "PacketZcNotifyEffect3" => (Some(canon_id(PacketZcNotifyEffect3::packet_id(v))), { let mut p = PacketZcNotifyEffect3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyEffect3::base_len(v)) }),
        "PacketZcNotifyExp" => (Some(canon_id(PacketZcNotifyExp::packet_id(v))), { let mut p = PacketZcNotifyExp::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyExp::base_len(v)) }),
        "PacketZcNotifyGroundskill" => (Some(canon_id(PacketZcNotifyGroundskill::packet_id(v))), { let mut p = PacketZcNotifyGroundskill::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyGroundskill::base_len(v)) }),
        "PacketZcNotifyHpToGroupm" => (Some(canon_id(PacketZcNotifyHpToGroupm::packet_id(v))), { let mut p = PacketZcNotifyHpToGroupm::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyHpToGroupm::base_len(v)) }),
        "PacketZcNotifyHpToGroupmR2" => (Some(canon_id(PacketZcNotifyHpToGroupmR2::packet_id(v))), { let mut p = PacketZcNotifyHpToGroupmR2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyHpToGroupmR2::base_len(v)) }),
        "PacketZcNotifyMapproperty" => (Some(canon_id(PacketZcNotifyMapproperty::packet_id(v))), { let mut p = PacketZcNotifyMapproperty::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyMapproperty::base_len(v)) }),
        "PacketZcNotifyMapproperty2" => (Some(canon_id(PacketZcNotifyMapproperty2::packet_id(v))), { let mut p = PacketZcNotifyMapproperty2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyMapproperty2::base_len(v)) }),
        "PacketZcNotifyMove" => (Some(canon_id(PacketZcNotifyMove::packet_id(v))), { let mut p = PacketZcNotifyMove::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyMove::base_len(v)) }),
        "PacketZcNotifyMoveentry" => (Some(canon_id(PacketZcNotifyMoveentry::packet_id(v))), { let mut p = PacketZcNotifyMoveentry::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyMoveentry::base_len(v)) }),
        "PacketZcNotifyMoveentry2" => (Some(canon_id(PacketZcNotifyMoveentry2::packet_id(v))), { let mut p = PacketZcNotifyMoveentry2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyMoveentry2::base_len(v)) }),
        "PacketZcNotifyMoveentry3" => (Some(canon_id(PacketZcNotifyMoveentry3::packet_id(v))), { let mut p = PacketZcNotifyMoveentry3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyMoveentry3::base_len(v)) }),
        "PacketZcNotifyMoveentry4" => (Some(canon_id(PacketZcNotifyMoveentry4::packet_id(v))), { let mut p = PacketZcNotifyMoveentry4::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyMoveentry4::base_len(v)) }),
        "PacketZcNotifyMoveentry7" => (Some(canon_id(PacketZcNotifyMoveentry7::packet_id(v))), { let mut p = PacketZcNotifyMoveentry7::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyMoveentry7::base_len(v)) }),
        "PacketZcNotifyMoveentry8" => (Some(canon_id(PacketZcNotifyMoveentry8::packet_id(v))), { let mut p = PacketZcNotifyMoveentry8::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyMoveentry8::base_len(v)) }),
        "PacketZcNotifyMoveentry9" => (Some(canon_id(PacketZcNotifyMoveentry9::packet_id(v))), { let mut p = PacketZcNotifyMoveentry9::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyMoveentry9::base_len(v)) }),
        "PacketZcNotifyNewentry" => (Some(canon_id(PacketZcNotifyNewentry::packet_id(v))), { let mut p = PacketZcNotifyNewentry::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyNewentry::base_len(v)) }),
        "PacketZcNotifyNewentry2" => (Some(canon_id(PacketZcNotifyNewentry2::packet_id(v))), { let mut p = PacketZcNotifyNewentry2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyNewentry2::base_len(v)) }),
        "PacketZcNotifyNewentry3" => (Some(canon_id(PacketZcNotifyNewentry3::packet_id(v))), { let mut p = PacketZcNotifyNewentry3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyNewentry3::base_len(v)) }),
        "PacketZcNotifyNewentry4" => (Some(canon_id(PacketZcNotifyNewentry4::packet_id(v))), { let mut p = PacketZcNotifyNewentry4::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyNewentry4::base_len(v)) }),
        "PacketZcNotifyNewentry5" => (Some(canon_id(PacketZcNotifyNewentry5::packet_id(v))), { let mut p = PacketZcNotifyNewentry5::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyNewentry5::base_len(v)) }),
        "PacketZcNotifyNewentry7" => (Some(canon_id(PacketZcNotifyNewentry7::packet_id(v))), { let mut p = PacketZcNotifyNewentry7::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyNewentry7::base_len(v)) }),
        "PacketZcNotifyPlayerchat" => (Some(canon_id(PacketZcNotifyPlayerchat::packet_id(v))), { let mut p = PacketZcNotifyPlayerchat::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyPlayerchat::base_len(v)) }),
        "PacketZcNotifyPlayermove" => (Some(canon_id(PacketZcNotifyPlayermove::packet_id(v))), { let mut p = PacketZcNotifyPlayermove::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyPlayermove::base_len(v)) }),
        "PacketZcNotifyPositionToGroupm" => (Some(canon_id(PacketZcNotifyPositionToGroupm::packet_id(v))), { let mut p = PacketZcNotifyPositionToGroupm::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyPositionToGroupm::base_len(v)) }),
        "PacketZcNotifyPositionToGuildm" => (Some(canon_id(PacketZcNotifyPositionToGuildm::packet_id(v))), { let mut p = PacketZcNotifyPositionToGuildm::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyPositionToGuildm::base_len(v)) }),
        "PacketZcNotifySkill" => (Some(canon_id(PacketZcNotifySkill::packet_id(v))), { let mut p = PacketZcNotifySkill::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifySkill::base_len(v)) }),
        "PacketZcNotifySkill2" => (Some(canon_id(PacketZcNotifySkill2::packet_id(v))), { let mut p = PacketZcNotifySkill2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifySkill2::base_len(v)) }),
        "PacketZcNotifyStandentry" => (Some(canon_id(PacketZcNotifyStandentry::packet_id(v))), { let mut p = PacketZcNotifyStandentry::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyStandentry::base_len(v)) }),
        "PacketZcNotifyStandentry2" => (Some(canon_id(PacketZcNotifyStandentry2::packet_id(v))), { let mut p = PacketZcNotifyStandentry2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyStandentry2::base_len(v)) }),
        "PacketZcNotifyStandentry3" => (Some(canon_id(PacketZcNotifyStandentry3::packet_id(v))), { let mut p = PacketZcNotifyStandentry3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyStandentry3::base_len(v)) }),
        "PacketZcNotifyStandentry4" => (Some(canon_id(PacketZcNotifyStandentry4::packet_id(v))), { let mut p = PacketZcNotifyStandentry4::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyStandentry4::base_len(v)) }),
        "PacketZcNotifyStandentry5" => (Some(canon_id(PacketZcNotifyStandentry5::packet_id(v))), { let mut p = PacketZcNotifyStandentry5::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyStandentry5::base_len(v)) }),
        "PacketZcNotifyStandentry6" => (Some(canon_id(PacketZcNotifyStandentry6::packet_id(v))), { let mut p = PacketZcNotifyStandentry6::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyStandentry6::base_len(v)) }),
        "PacketZcNotifyStandentry7" => (Some(canon_id(PacketZcNotifyStandentry7::packet_id(v))), { let mut p = PacketZcNotifyStandentry7::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyStandentry7::base_len(v)) }),
        "PacketZcNotifyStoreitemCountinfo" => (Some(canon_id(PacketZcNotifyStoreitemCountinfo::packet_id(v))), { let mut p = PacketZcNotifyStoreitemCountinfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyStoreitemCountinfo::base_len(v)) }),
        "PacketZcNotifyTime" => (Some(canon_id(PacketZcNotifyTime::packet_id(v))), { let mut p = PacketZcNotifyTime::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyTime::base_len(v)) }),
        "PacketZcNotifyVanish" => (Some(canon_id(PacketZcNotifyVanish::packet_id(v))), { let mut p = PacketZcNotifyVanish::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyVanish::base_len(v)) }),
        "PacketZcNotifyWeaponitemlist" => (Some(canon_id(PacketZcNotifyWeaponitemlist::packet_id(v))), { let mut p = PacketZcNotifyWeaponitemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNotifyWeaponitemlist::base_len(v)) }),
        "PacketZcNpcackMapmove" => (Some(canon_id(PacketZcNpcackMapmove::packet_id(v))), { let mut p = PacketZcNpcackMapmove::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNpcackMapmove::base_len(v)) }),
        "PacketZcNpcspriteChange" => (Some(canon_id(PacketZcNpcspriteChange::packet_id(v))), { let mut p = PacketZcNpcspriteChange::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcNpcspriteChange::base_len(v)) }),
        "PacketZcOpenEditdlg" => (Some(canon_id(PacketZcOpenEditdlg::packet_id(v))), { let mut p = PacketZcOpenEditdlg::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcOpenEditdlg::base_len(v)) }),
        "PacketZcOpenEditdlgstr" => (Some(canon_id(PacketZcOpenEditdlgstr::packet_id(v))), { let mut p = PacketZcOpenEditdlgstr::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcOpenEditdlgstr::base_len(v)) }),
        "PacketZcOpenstore" => (Some(canon_id(PacketZcOpenstore::packet_id(v))), { let mut p = PacketZcOpenstore::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcOpenstore::base_len(v)) }),
        "PacketZcOtherGuildList" => (Some(canon_id(PacketZcOtherGuildList::packet_id(v))), { let mut p = PacketZcOtherGuildList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcOtherGuildList::base_len(v)) }),
        "PacketZcParChange" => (Some(canon_id(PacketZcParChange::packet_id(v))), { let mut p = PacketZcParChange::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcParChange::base_len(v)) }),
        "PacketZcPartyConfig" => (Some(canon_id(PacketZcPartyConfig::packet_id(v))), { let mut p = PacketZcPartyConfig::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPartyConfig::base_len(v)) }),
        "PacketZcPartyJoinReq" => (Some(canon_id(PacketZcPartyJoinReq::packet_id(v))), { let mut p = PacketZcPartyJoinReq::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPartyJoinReq::base_len(v)) }),
        "PacketZcPartyJoinReqAck" => (Some(canon_id(PacketZcPartyJoinReqAck::packet_id(v))), { let mut p = PacketZcPartyJoinReqAck::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPartyJoinReqAck::base_len(v)) }),
        "PacketZcPcPurchaseItemlist" => (Some(canon_id(PacketZcPcPurchaseItemlist::packet_id(v))), { let mut p = PacketZcPcPurchaseItemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPcPurchaseItemlist::base_len(v)) }),
        "PacketZcPcPurchaseItemlistFrommc2" => (Some(canon_id(PacketZcPcPurchaseItemlistFrommc2::packet_id(v))), { let mut p = PacketZcPcPurchaseItemlistFrommc2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPcPurchaseItemlistFrommc2::base_len(v)) }),
        "PacketZcPcPurchaseMyitemlist" => (Some(canon_id(PacketZcPcPurchaseMyitemlist::packet_id(v))), { let mut p = PacketZcPcPurchaseMyitemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPcPurchaseMyitemlist::base_len(v)) }),
        "PacketZcPcPurchaseResult" => (Some(canon_id(PacketZcPcPurchaseResult::packet_id(v))), { let mut p = PacketZcPcPurchaseResult::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPcPurchaseResult::base_len(v)) }),
        "PacketZcPcPurchaseResultFrommc" => (Some(canon_id(PacketZcPcPurchaseResultFrommc::packet_id(v))), { let mut p = PacketZcPcPurchaseResultFrommc::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPcPurchaseResultFrommc::base_len(v)) }),
        "PacketZcPcSellItemlist" => (Some(canon_id(PacketZcPcSellItemlist::packet_id(v))), { let mut p = PacketZcPcSellItemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPcSellItemlist::base_len(v)) }),
        "PacketZcPcSellResult" => (Some(canon_id(PacketZcPcSellResult::packet_id(v))), { let mut p = PacketZcPcSellResult::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPcSellResult::base_len(v)) }),
        "PacketZcPetAct" => (Some(canon_id(PacketZcPetAct::packet_id(v))), { let mut p = PacketZcPetAct::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPetAct::base_len(v)) }),
        "PacketZcPeteggList" => (Some(canon_id(PacketZcPeteggList::packet_id(v))), { let mut p = PacketZcPeteggList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPeteggList::base_len(v)) }),
        "PacketZcPositionIdNameInfo" => (Some(canon_id(PacketZcPositionIdNameInfo::packet_id(v))), { let mut p = PacketZcPositionIdNameInfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPositionIdNameInfo::base_len(v)) }),
        "PacketZcPositionInfo" => (Some(canon_id(PacketZcPositionInfo::packet_id(v))), { let mut p = PacketZcPositionInfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPositionInfo::base_len(v)) }),
        "PacketZcProgressCancel" => (Some(canon_id(PacketZcProgressCancel::packet_id(v))), { let mut p = PacketZcProgressCancel::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcProgressCancel::base_len(v)) }),
        "PacketZcPropertyHomun" => (Some(canon_id(PacketZcPropertyHomun::packet_id(v))), { let mut p = PacketZcPropertyHomun::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPropertyHomun::base_len(v)) }),
        "PacketZcPropertyPet" => (Some(canon_id(PacketZcPropertyPet::packet_id(v))), { let mut p = PacketZcPropertyPet::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcPropertyPet::base_len(v)) }),
        "PacketZcQuestInfo" => (None, { let mut p = PacketZcQuestInfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcQuestInfo::base_len(v)) }),
        "PacketZcQuestMissionInfo" => (None, { let mut p = PacketZcQuestMissionInfo::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcQuestMissionInfo::base_len(v)) }),
        "PacketZcQuestNotifyEffect" => (Some(canon_id(PacketZcQuestNotifyEffect::packet_id(v))), { let mut p = PacketZcQuestNotifyEffect::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcQuestNotifyEffect::base_len(v)) }),
        "PacketZcRecovery" => (Some(canon_id(PacketZcRecovery::packet_id(v))), { let mut p = PacketZcRecovery::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcRecovery::base_len(v)) }),
        "PacketZcRefuseEnterRoom" => (Some(canon_id(PacketZcRefuseEnterRoom::packet_id(v))), { let mut p = PacketZcRefuseEnterRoom::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcRefuseEnterRoom::base_len(v)) }),
        "PacketZcRepairitemlist" => (Some(canon_id(PacketZcRepairitemlist::packet_id(v))), { let mut p = PacketZcRepairitemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcRepairitemlist::base_len(v)) }),
        "PacketZcReqAddFriends" => (Some(canon_id(PacketZcReqAddFriends::packet_id(v))), { let mut p = PacketZcReqAddFriends::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqAddFriends::base_len(v)) }),
        "PacketZcReqAllyGuild" => (Some(canon_id(PacketZcReqAllyGuild::packet_id(v))), { let mut p = PacketZcReqAllyGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqAllyGuild::base_len(v)) }),
        "PacketZcReqBaby" => (Some(canon_id(PacketZcReqBaby::packet_id(v))), { let mut p = PacketZcReqBaby::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqBaby::base_len(v)) }),
        "PacketZcReqCouple" => (Some(canon_id(PacketZcReqCouple::packet_id(v))), { let mut p = PacketZcReqCouple::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqCouple::base_len(v)) }),
        "PacketZcReqExchangeItem" => (Some(canon_id(PacketZcReqExchangeItem::packet_id(v))), { let mut p = PacketZcReqExchangeItem::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqExchangeItem::base_len(v)) }),
        "PacketZcReqExchangeItem2" => (Some(canon_id(PacketZcReqExchangeItem2::packet_id(v))), { let mut p = PacketZcReqExchangeItem2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqExchangeItem2::base_len(v)) }),
        "PacketZcReqGroupinfoChangeV2" => (Some(canon_id(PacketZcReqGroupinfoChangeV2::packet_id(v))), { let mut p = PacketZcReqGroupinfoChangeV2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqGroupinfoChangeV2::base_len(v)) }),
        "PacketZcReqJoinGroup" => (Some(canon_id(PacketZcReqJoinGroup::packet_id(v))), { let mut p = PacketZcReqJoinGroup::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqJoinGroup::base_len(v)) }),
        "PacketZcReqJoinGuild" => (Some(canon_id(PacketZcReqJoinGuild::packet_id(v))), { let mut p = PacketZcReqJoinGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqJoinGuild::base_len(v)) }),
        "PacketZcReqStorePassword" => (Some(canon_id(PacketZcReqStorePassword::packet_id(v))), { let mut p = PacketZcReqStorePassword::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqStorePassword::base_len(v)) }),
        "PacketZcReqTakeoffEquipAck" => (Some(canon_id(PacketZcReqTakeoffEquipAck::packet_id(v))), { let mut p = PacketZcReqTakeoffEquipAck::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqTakeoffEquipAck::base_len(v)) }),
        "PacketZcReqTakeoffEquipAck2" => (Some(canon_id(PacketZcReqTakeoffEquipAck2::packet_id(v))), { let mut p = PacketZcReqTakeoffEquipAck2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqTakeoffEquipAck2::base_len(v)) }),
        "PacketZcReqWearEquipAck" => (Some(canon_id(PacketZcReqWearEquipAck::packet_id(v))), { let mut p = PacketZcReqWearEquipAck::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqWearEquipAck::base_len(v)) }),
        "PacketZcReqWearEquipAck2" => (Some(canon_id(PacketZcReqWearEquipAck2::packet_id(v))), { let mut p = PacketZcReqWearEquipAck2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcReqWearEquipAck2::base_len(v)) }),
        "PacketZcRestartAck" => (Some(canon_id(PacketZcRestartAck::packet_id(v))), { let mut p = PacketZcRestartAck::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcRestartAck::base_len(v)) }),
        "PacketZcResultMakeGuild" => (Some(canon_id(PacketZcResultMakeGuild::packet_id(v))), { let mut p = PacketZcResultMakeGuild::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcResultMakeGuild::base_len(v)) }),
        "PacketZcResultStorePassword" => (Some(canon_id(PacketZcResultStorePassword::packet_id(v))), { let mut p = PacketZcResultStorePassword::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcResultStorePassword::base_len(v)) }),
        "PacketZcResurrection" => (Some(canon_id(PacketZcResurrection::packet_id(v))), { let mut p = PacketZcResurrection::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcResurrection::base_len(v)) }),
        "PacketZcRoleChange" => (Some(canon_id(PacketZcRoleChange::packet_id(v))), { let mut p = PacketZcRoleChange::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcRoleChange::base_len(v)) }),
        "PacketZcRoomNewentry" => (Some(canon_id(PacketZcRoomNewentry::packet_id(v))), { let mut p = PacketZcRoomNewentry::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcRoomNewentry::base_len(v)) }),
        "PacketZcSayDialog" => (Some(canon_id(PacketZcSayDialog::packet_id(v))), { let mut p = PacketZcSayDialog::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSayDialog::base_len(v)) }),
        "PacketZcSelectDealtype" => (Some(canon_id(PacketZcSelectDealtype::packet_id(v))), { let mut p = PacketZcSelectDealtype::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSelectDealtype::base_len(v)) }),
        "PacketZcShortcutKeyList" => (Some(canon_id(PacketZcShortcutKeyList::packet_id(v))), { let mut p = PacketZcShortcutKeyList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcShortcutKeyList::base_len(v)) }),
        "PacketZcShortcutKeyListV2" => (Some(canon_id(PacketZcShortcutKeyListV2::packet_id(v))), { let mut p = PacketZcShortcutKeyListV2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcShortcutKeyListV2::base_len(v)) }),
        "PacketZcShowImage2" => (Some(canon_id(PacketZcShowImage2::packet_id(v))), { let mut p = PacketZcShowImage2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcShowImage2::base_len(v)) }),
        "PacketZcSkillDisappear" => (Some(canon_id(PacketZcSkillDisappear::packet_id(v))), { let mut p = PacketZcSkillDisappear::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSkillDisappear::base_len(v)) }),
        "PacketZcSkillEntry" => (Some(canon_id(PacketZcSkillEntry::packet_id(v))), { let mut p = PacketZcSkillEntry::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSkillEntry::base_len(v)) }),
        "PacketZcSkillPostdelay" => (Some(canon_id(PacketZcSkillPostdelay::packet_id(v))), { let mut p = PacketZcSkillPostdelay::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSkillPostdelay::base_len(v)) }),
        "PacketZcSkillUpdate" => (Some(canon_id(PacketZcSkillUpdate::packet_id(v))), { let mut p = PacketZcSkillUpdate::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSkillUpdate::base_len(v)) }),
        "PacketZcSkillinfoList" => (Some(canon_id(PacketZcSkillinfoList::packet_id(v))), { let mut p = PacketZcSkillinfoList::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSkillinfoList::base_len(v)) }),
        "PacketZcSkillinfoUpdate" => (Some(canon_id(PacketZcSkillinfoUpdate::packet_id(v))), { let mut p = PacketZcSkillinfoUpdate::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSkillinfoUpdate::base_len(v)) }),
        "PacketZcSkillinfoUpdate2" => (Some(canon_id(PacketZcSkillinfoUpdate2::packet_id(v))), { let mut p = PacketZcSkillinfoUpdate2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSkillinfoUpdate2::base_len(v)) }),
        "PacketZcSound" => (Some(canon_id(PacketZcSound::packet_id(v))), { let mut p = PacketZcSound::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSound::base_len(v)) }),
        "PacketZcSpirits" => (Some(canon_id(PacketZcSpirits::packet_id(v))), { let mut p = PacketZcSpirits::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSpirits::base_len(v)) }),
        "PacketZcSpirits2" => (Some(canon_id(PacketZcSpirits2::packet_id(v))), { let mut p = PacketZcSpirits2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSpirits2::base_len(v)) }),
        "PacketZcSpriteChange" => (Some(canon_id(PacketZcSpriteChange::packet_id(v))), { let mut p = PacketZcSpriteChange::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSpriteChange::base_len(v)) }),
        "PacketZcSpriteChange2" => (Some(canon_id(PacketZcSpriteChange2::packet_id(v))), { let mut p = PacketZcSpriteChange2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcSpriteChange2::base_len(v)) }),
        "PacketZcStartCapture" => (Some(canon_id(PacketZcStartCapture::packet_id(v))), { let mut p = PacketZcStartCapture::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStartCapture::base_len(v)) }),
        "PacketZcStartCouple" => (Some(canon_id(PacketZcStartCouple::packet_id(v))), { let mut p = PacketZcStartCouple::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStartCouple::base_len(v)) }),
        "PacketZcStateChange3" => (Some(canon_id(PacketZcStateChange3::packet_id(v))), { let mut p = PacketZcStateChange3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStateChange3::base_len(v)) }),
        "PacketZcStatus" => (Some(canon_id(PacketZcStatus::packet_id(v))), { let mut p = PacketZcStatus::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStatus::base_len(v)) }),
        "PacketZcStatusValues" => (Some(canon_id(PacketZcStatusValues::packet_id(v))), { let mut p = PacketZcStatusValues::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStatusValues::base_len(v)) }),
        "PacketZcStopmove" => (Some(canon_id(PacketZcStopmove::packet_id(v))), { let mut p = PacketZcStopmove::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStopmove::base_len(v)) }),
        "PacketZcStoreEntry" => (Some(canon_id(PacketZcStoreEntry::packet_id(v))), { let mut p = PacketZcStoreEntry::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStoreEntry::base_len(v)) }),
        "PacketZcStoreEquipmentItemlist" => (Some(canon_id(PacketZcStoreEquipmentItemlist::packet_id(v))), { let mut p = PacketZcStoreEquipmentItemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStoreEquipmentItemlist::base_len(v)) }),
        "PacketZcStoreEquipmentItemlist2" => (Some(canon_id(PacketZcStoreEquipmentItemlist2::packet_id(v))), { let mut p = PacketZcStoreEquipmentItemlist2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStoreEquipmentItemlist2::base_len(v)) }),
        "PacketZcStoreEquipmentItemlist3" => (Some(canon_id(PacketZcStoreEquipmentItemlist3::packet_id(v))), { let mut p = PacketZcStoreEquipmentItemlist3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStoreEquipmentItemlist3::base_len(v)) }),
        "PacketZcStoreNormalItemlist" => (Some(canon_id(PacketZcStoreNormalItemlist::packet_id(v))), { let mut p = PacketZcStoreNormalItemlist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStoreNormalItemlist::base_len(v)) }),
        "PacketZcStoreNormalItemlist2" => (Some(canon_id(PacketZcStoreNormalItemlist2::packet_id(v))), { let mut p = PacketZcStoreNormalItemlist2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStoreNormalItemlist2::base_len(v)) }),
        "PacketZcStoreNormalItemlist3" => (Some(canon_id(PacketZcStoreNormalItemlist3::packet_id(v))), { let mut p = PacketZcStoreNormalItemlist3::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcStoreNormalItemlist3::base_len(v)) }),
        "PacketZcTaekwonRank" => (Some(canon_id(PacketZcTaekwonRank::packet_id(v))), { let mut p = PacketZcTaekwonRank::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcTaekwonRank::base_len(v)) }),
        "PacketZcTrycaptureMonster" => (Some(canon_id(PacketZcTrycaptureMonster::packet_id(v))), { let mut p = PacketZcTrycaptureMonster::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcTrycaptureMonster::base_len(v)) }),
        "PacketZcUpdateGdid" => (Some(canon_id(PacketZcUpdateGdid::packet_id(v))), { let mut p = PacketZcUpdateGdid::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcUpdateGdid::base_len(v)) }),
        "PacketZcUpdateMissionHunt" => (Some(canon_id(PacketZcUpdateMissionHunt::packet_id(v))), { let mut p = PacketZcUpdateMissionHunt::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcUpdateMissionHunt::base_len(v)) }),
        "PacketZcUseItemAck" => (Some(canon_id(PacketZcUseItemAck::packet_id(v))), { let mut p = PacketZcUseItemAck::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcUseItemAck::base_len(v)) }),
        "PacketZcUseItemAck2" => (Some(canon_id(PacketZcUseItemAck2::packet_id(v))), { let mut p = PacketZcUseItemAck2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcUseItemAck2::base_len(v)) }),
        "PacketZcUseSkill" => (Some(canon_id(PacketZcUseSkill::packet_id(v))), { let mut p = PacketZcUseSkill::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcUseSkill::base_len(v)) }),
        "PacketZcUserCount" => (Some(canon_id(PacketZcUserCount::packet_id(v))), { let mut p = PacketZcUserCount::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcUserCount::base_len(v)) }),
        "PacketZcUseskillAck" => (Some(canon_id(PacketZcUseskillAck::packet_id(v))), { let mut p = PacketZcUseskillAck::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcUseskillAck::base_len(v)) }),
        "PacketZcUseskillAck2" => (Some(canon_id(PacketZcUseskillAck2::packet_id(v))), { let mut p = PacketZcUseskillAck2::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcUseskillAck2::base_len(v)) }),
        "PacketZcWaitDialog" => (Some(canon_id(PacketZcWaitDialog::packet_id(v))), { let mut p = PacketZcWaitDialog::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcWaitDialog::base_len(v)) }),
        "PacketZcWarplist" => (Some(canon_id(PacketZcWarplist::packet_id(v))), { let mut p = PacketZcWarplist::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcWarplist::base_len(v)) }),
        "PacketZcWhisper" => (Some(canon_id(PacketZcWhisper::packet_id(v))), { let mut p = PacketZcWhisper::new(v); p.fill_raw_with_packetver(Some(v)); p.raw.len().max(PacketZcWhisper::base_len(v)) }),
        _ => return None,
    })
}

// ---- minimal C-preprocessor emulation (MAIN classic build) ----

#[derive(Clone, Copy)]
struct Frame {
    this_active: bool,
    any_taken: bool,
}

struct Pp {
    stack: Vec<Frame>,
}

impl Pp {
    fn new() -> Self {
        Pp { stack: Vec::new() }
    }
    fn active(&self) -> bool {
        self.stack.iter().all(|f| f.this_active)
    }
    fn parent_active(&self) -> bool {
        self.stack.iter().all(|f| f.this_active)
    }
    fn handle(&mut self, line: &str, target: u32) -> bool {
        // returns true if `line` is a directive (consumed)
        let l = line.trim();
        if l.starts_with("#if ") || l == "#if" || l.starts_with("#ifdef") || l.starts_with("#ifndef") {
            let parent = self.parent_active();
            let cond = if let Some(rest) = l.strip_prefix("#ifndef") {
                !defined(rest.trim())
            } else if let Some(rest) = l.strip_prefix("#ifdef") {
                defined(rest.trim())
            } else {
                eval_cond(l.trim_start_matches("#if"), target)
            };
            let a = parent && cond;
            self.stack.push(Frame { this_active: a, any_taken: a });
            return true;
        }
        if l.starts_with("#elif") {
            if let Some(top) = self.stack.pop() {
                let parent = self.parent_active();
                let cond = eval_cond(l.trim_start_matches("#elif"), target);
                let a = parent && !top.any_taken && cond;
                self.stack.push(Frame { this_active: a, any_taken: top.any_taken || a });
            }
            return true;
        }
        if l.starts_with("#else") {
            if let Some(top) = self.stack.pop() {
                let parent = self.parent_active();
                let a = parent && !top.any_taken;
                self.stack.push(Frame { this_active: a, any_taken: true });
            }
            return true;
        }
        if l.starts_with("#endif") {
            self.stack.pop();
            return true;
        }
        l.starts_with('#')
    }
}

// MAIN classic build: PACKETVER=PACKETVER_MAIN_NUM=target; RE_NUM=ZERO_NUM=0;
// PACKETVER(+_MAIN) defined, RE/ZERO undefined.
fn defined(sym: &str) -> bool {
    let s = sym.trim();
    s == "PACKETVER" || s == "PACKETVER_MAIN"
}

fn eval_cond(expr: &str, target: u32) -> bool {
    let e = expr.trim();
    // handle a leading negation of a (parenthesized) group before splitting
    if let Some(rest) = e.strip_prefix('!') {
        let r = rest.trim();
        let inner = if r.starts_with('(') && r.ends_with(')') {
            &r[1..r.len() - 1]
        } else {
            r
        };
        return !eval_cond(inner, target);
    }
    // OR of AND of atoms
    e.split("||").any(|ort| ort.split("&&").all(|a| eval_atom(a.trim(), target)))
}

fn eval_atom(atom: &str, target: u32) -> bool {
    let a = atom.trim().trim_start_matches('(').trim_end_matches(')').trim();
    if let Some(inner) = a.strip_prefix("defined") {
        let name = inner.trim().trim_start_matches('(').trim_end_matches(')').trim();
        return defined(name);
    }
    if let Some(inner) = a.strip_prefix("!defined") {
        let name = inner.trim().trim_start_matches('(').trim_end_matches(')').trim();
        return !defined(name);
    }
    // <ident> <op> <num>
    for op in ["<=", ">=", "==", "!=", "<", ">"] {
        if let Some(idx) = a.find(op) {
            let lhs = a[..idx].trim();
            let rhs = a[idx + op.len()..].trim();
            let n: u64 = match rhs.parse() {
                Ok(n) => n,
                Err(_) => return false,
            };
            let lv: u64 = match lhs {
                "PACKETVER" | "PACKETVER_MAIN_NUM" => target as u64,
                "PACKETVER_RE_NUM" | "PACKETVER_ZERO_NUM" | "PACKETVER_SAK_NUM" => 0,
                _ => return false,
            };
            return match op {
                "<=" => lv <= n,
                ">=" => lv >= n,
                "==" => lv == n,
                "!=" => lv != n,
                "<" => lv < n,
                ">" => lv > n,
                _ => false,
            };
        }
    }
    false
}

// ---- reference builders ----

#[derive(Clone, Copy, Debug, PartialEq)]
enum RefLen {
    Fixed(i32),
    Var,        // variable-length ([] / PacketLength)
    Unresolved, // sizeof()/unknown sub-struct/unknown array constant
}

#[derive(Clone, Debug)]
struct RefEntry {
    is_cz: bool,
    len: RefLen,
    src: &'static str,
}

// Merged id -> reference at target. Priority: rathena packets.hpp,
// Hercules packets_struct.h, then legacy rathena clif_packetdb.hpp.
fn build_reference(target: u32) -> Option<BTreeMap<u16, RefEntry>> {
    let mut map: BTreeMap<u16, RefEntry> = BTreeMap::new();
    let mut any = false;
    if let Some(m) = parse_struct_source(
        "../../../rathena/src/map/packets.hpp",
        target,
        "ra:packets.hpp",
    ) {
        any = true;
        merge(&mut map, m);
    }
    if let Some(m) = parse_struct_source(
        "../../../Hercules/src/map/packets_struct.h",
        target,
        "he:packets_struct.h",
    ) {
        any = true;
        merge(&mut map, m);
    }
    if let Some(m) = parse_clif_packetdb(target) {
        any = true;
        merge(&mut map, m);
    }
    // gap-fill AMBIG (Unresolved) entries from clif.cpp /// layout comments;
    // do NOT insert absent ids (modern comment layout would false-flag legacy variants).
    if let Some(m) = parse_clif_comments() {
        merge_unresolved(&mut map, m);
    }
    if any { Some(map) } else { None }
}

fn merge_unresolved(dst: &mut BTreeMap<u16, RefEntry>, src: BTreeMap<u16, RefEntry>) {
    for (id, e) in src {
        if let Some(prev) = dst.get(&id) {
            if prev.len == RefLen::Unresolved && e.len != RefLen::Unresolved {
                dst.insert(id, e);
            }
        }
    }
}

// sum `.<count><TYPE>` field tokens (B=1,W=2,L=4,Q=8) in a /// layout string
fn sum_layout_tokens(s: &str) -> usize {
    let b = s.as_bytes();
    let mut total = 0usize;
    let mut i = 0;
    while i < b.len() {
        if b[i] == b'.' {
            let mut j = i + 1;
            let mut count = 0usize;
            let mut has_digit = false;
            while j < b.len() && b[j].is_ascii_digit() {
                count = count * 10 + (b[j] - b'0') as usize;
                j += 1;
                has_digit = true;
            }
            if j < b.len() {
                let base = match b[j] {
                    b'B' => Some(1usize),
                    b'W' => Some(2),
                    b'L' => Some(4),
                    b'Q' => Some(8),
                    _ => None,
                };
                let end_ok = j + 1 >= b.len() || !(b[j + 1] as char).is_ascii_alphanumeric();
                if let (Some(base), true) = (base, end_ok) {
                    let c = if has_digit && count > 0 { count } else { 1 };
                    total += c * base;
                    i = j + 1;
                    continue;
                }
            }
        }
        i += 1;
    }
    total
}

fn clif_layout_len(body: &str) -> RefLen {
    let lower = body.to_ascii_lowercase();
    if lower.contains("*?") || lower.contains("<packet len") || lower.contains("<len>")
        || lower.contains("<length")
    {
        return RefLen::Var;
    }
    let mut extra = 0usize;
    let mut idx = 0;
    while let Some(pos) = body[idx..].find("}*") {
        let p = idx + pos;
        let num: String = body[p + 2..].chars().take_while(|c| c.is_ascii_digit()).collect();
        if num.is_empty() {
            return RefLen::Var;
        }
        let n: usize = num.parse().unwrap_or(1);
        if let Some(open) = body[..p].rfind('{') {
            let bs = sum_layout_tokens(&body[open + 1..p]);
            extra += bs.saturating_mul(n.saturating_sub(1));
        }
        idx = p + 2;
    }
    RefLen::Fixed((2 + sum_layout_tokens(body) + extra) as i32)
}

// rathena clif.cpp `/// <id> <fields...> (NAME)` doc-comments -> id -> length.
// Not version-gated (modern layout); used only to fill AMBIG gaps.
fn parse_clif_comments() -> Option<BTreeMap<u16, RefEntry>> {
    let text = std::fs::read_to_string(manifest_dir().join("../../../rathena/src/map/clif.cpp")).ok()?;
    let mut map: BTreeMap<u16, RefEntry> = BTreeMap::new();
    for raw in text.lines() {
        let t = raw.trim();
        let rest = match t.strip_prefix("/// ") {
            Some(r) => r,
            None => continue,
        };
        let mut it = rest.splitn(2, ' ');
        let id_tok = it.next().unwrap_or("");
        let body_all = it.next().unwrap_or("");
        if id_tok.len() != 4 || !id_tok.bytes().all(|c| c.is_ascii_hexdigit()) {
            continue;
        }
        let id = match u16::from_str_radix(id_tok, 16) {
            Ok(v) => v,
            Err(_) => continue,
        };
        // strip trailing (NAME) and detect direction
        let (body, name) = match body_all.rfind('(') {
            Some(o) => (&body_all[..o], &body_all[o + 1..]),
            None => (body_all, ""),
        };
        let is_cz = name.starts_with("CZ_");
        map.entry(id).or_insert(RefEntry {
            is_cz,
            len: clif_layout_len(body),
            src: "ra:clif.cpp",
        });
    }
    Some(map)
}

fn merge(dst: &mut BTreeMap<u16, RefEntry>, src: BTreeMap<u16, RefEntry>) {
    for (id, e) in src {
        match dst.get(&id) {
            None => {
                dst.insert(id, e);
            }
            // upgrade a prior unresolved reference to a resolved one
            Some(prev) if prev.len == RefLen::Unresolved && e.len != RefLen::Unresolved => {
                dst.insert(id, e);
            }
            Some(_) => {}
        }
    }
}

// Known array-length constants and cross-file sub-struct sizes (classic band).
fn const_val(s: &str) -> Option<i64> {
    let s = s.trim();
    match s {
        "NAME_LENGTH" => Some(24),
        "MAP_NAME_LENGTH" => Some(12),
        "MAP_NAME_LENGTH_EXT" => Some(16),
        "MESSAGE_SIZE" => Some(80),
        "CHAT_SIZE_MAX" => Some(256),
        "CHATROOM_TITLE_SIZE" => Some(37),
        "TALKBOX_MESSAGE_SIZE" => Some(80),
        _ => parse_int(s).map(|v| v as i64),
    }
}

fn prim_size(t: &str) -> Option<usize> {
    Some(match t {
        "int8" | "uint8" | "char" | "bool" => 1,
        "int16" | "uint16" | "short" => 2,
        "int32" | "uint32" | "int" | "long" | "float" => 4,
        "int64" | "uint64" => 8,
        _ => return None,
    })
}

// Parse a C struct source: PACKET_*_ structs (sizeof) + DEFINE_PACKET_HEADER/ID.
fn parse_struct_source(
    rel: &str,
    target: u32,
    src: &'static str,
) -> Option<BTreeMap<u16, RefEntry>> {
    let text = std::fs::read_to_string(manifest_dir().join(rel)).ok()?;
    let mut sizes: BTreeMap<String, RefLen> = BTreeMap::new();
    sizes.insert("EQUIPSLOTINFO".into(), RefLen::Fixed(8)); // uint16 card[4], classic
    let mut ids: Vec<(String, u16)> = Vec::new();
    let mut pp = Pp::new();

    let mut cur: Option<String> = None;
    let mut acc: i64 = 0;
    let mut is_var = false;
    let mut unresolved = false;

    for raw in text.lines() {
        let line = strip_comment(raw);
        if pp.handle(&line, target) {
            continue;
        }
        if !pp.active() {
            continue;
        }
        let t = line.trim();

        if let Some(d) = parse_define(t) {
            ids.push(d);
            continue;
        }

        if cur.is_none() {
            if let Some(name) = parse_struct_open(t) {
                cur = Some(name);
                acc = 0;
                is_var = false;
                unresolved = false;
            }
            continue;
        }

        // inside a struct body
        if t.starts_with('}') {
            let name = cur.take().unwrap();
            let len = if is_var {
                RefLen::Var
            } else if unresolved {
                RefLen::Unresolved
            } else {
                RefLen::Fixed(acc as i32)
            };
            sizes.insert(name, len);
            continue;
        }
        if let Some(name) = parse_struct_open(t) {
            // inline nested struct definition: treat conservatively as unresolved
            let _ = name;
            unresolved = true;
            continue;
        }
        // a PacketLength field is the canonical marker of a variable-length packet
        if t.to_ascii_lowercase().contains("packetlength") {
            is_var = true;
            continue;
        }
        match field_size(t, &sizes) {
            FieldSize::Bytes(n) => acc += n as i64,
            FieldSize::Var => is_var = true,
            FieldSize::Unresolved => unresolved = true,
            FieldSize::None => {}
        }
    }

    let mut out: BTreeMap<u16, RefEntry> = BTreeMap::new();
    for (name, id) in ids {
        let is_cz = name.starts_with("CZ_");
        let len = sizes
            .get(&format!("PACKET_{}", name))
            .copied()
            .unwrap_or(RefLen::Unresolved);
        out.entry(id).or_insert(RefEntry { is_cz, len, src });
    }
    Some(out)
}

fn parse_define(t: &str) -> Option<(String, u16)> {
    let inner = t
        .strip_prefix("DEFINE_PACKET_HEADER(")
        .or_else(|| t.strip_prefix("DEFINE_PACKET_ID("))?;
    let close = inner.find(')')?;
    let args: Vec<&str> = inner[..close].split(',').map(|s| s.trim()).collect();
    if args.len() < 2 {
        return None;
    }
    let name = args[0].to_string();
    let id = parse_int(args[1])? as u16;
    Some((name, id))
}

fn parse_struct_open(t: &str) -> Option<String> {
    let rest = t.strip_prefix("struct ")?;
    let rest = rest.trim();
    let end = rest
        .find(|c: char| c == '{' || c.is_whitespace())
        .unwrap_or(rest.len());
    let name = &rest[..end];
    if !rest[end..].trim_start().starts_with('{') {
        return None;
    }
    if name.is_empty() {
        return None;
    }
    Some(name.to_string())
}

enum FieldSize {
    Bytes(usize),
    Var,
    Unresolved,
    None,
}

fn field_size(t: &str, sizes: &BTreeMap<String, RefLen>) -> FieldSize {
    let t = t.trim().trim_end_matches(';').trim();
    if t.is_empty() {
        return FieldSize::None;
    }
    let mut toks: Vec<&str> = t.split_whitespace().collect();
    if toks.first() == Some(&"struct") {
        toks.remove(0);
    }
    if toks.len() < 2 {
        return FieldSize::None;
    }
    let ty = toks[0];
    let decl = toks[1..].join("");
    // dimensions in the declarator
    let mut count: i64 = 1;
    let mut rest = decl.as_str();
    while let Some(o) = rest.find('[') {
        let c = match rest[o..].find(']') {
            Some(i) => o + i,
            None => return FieldSize::Unresolved,
        };
        let dim = &rest[o + 1..c];
        if dim.trim().is_empty() {
            return FieldSize::Var;
        }
        match const_val(dim) {
            Some(v) => count *= v,
            None => return FieldSize::Unresolved,
        }
        rest = &rest[c + 1..];
    }
    let base = if let Some(p) = prim_size(ty) {
        p
    } else {
        match sizes.get(ty) {
            Some(RefLen::Fixed(n)) => *n as usize,
            Some(RefLen::Var) => return FieldSize::Var,
            _ => return FieldSize::Unresolved,
        }
    };
    FieldSize::Bytes(base * count as usize)
}

// Resolve version-gated symbolic ids from rathena packets_struct.hpp enum.
fn symbol_ids(target: u32) -> BTreeMap<String, u16> {
    let path = manifest_dir().join("../../../rathena/src/map/packets_struct.hpp");
    let mut map = BTreeMap::new();
    let text = match std::fs::read_to_string(&path) {
        Ok(t) => t,
        Err(_) => return map,
    };
    let mut pp = Pp::new();
    for raw in text.lines() {
        let line = strip_comment(raw);
        if pp.handle(&line, target) {
            continue;
        }
        if !pp.active() {
            continue;
        }
        if let Some(eq) = line.find('=') {
            let name = line[..eq].trim();
            if name.is_empty() || !name.chars().all(|c| c.is_ascii_alphanumeric() || c == '_') {
                continue;
            }
            let val = line[eq + 1..].trim().trim_end_matches(',').trim();
            if let Some(id) = parse_int(val) {
                if id <= 0xffff {
                    map.insert(name.to_string(), id as u16);
                }
            }
        }
    }
    map
}

// Legacy rathena clif_packetdb.hpp: cumulative, last-wins packet()/parseable_packet().
fn parse_clif_packetdb(target: u32) -> Option<BTreeMap<u16, RefEntry>> {
    let path = manifest_dir().join("../../../rathena/src/map/clif_packetdb.hpp");
    let text = std::fs::read_to_string(&path).ok()?;
    let syms = symbol_ids(target);
    let mut map: BTreeMap<u16, RefEntry> = BTreeMap::new();
    let mut pp = Pp::new();
    for raw in text.lines() {
        let line = strip_comment(raw);
        if pp.handle(&line, target) {
            continue;
        }
        if !pp.active() {
            continue;
        }
        let t = line.trim();
        let is_cz = t.starts_with("parseable_packet");
        let is_zc = t.starts_with("packet(") || t.starts_with("packet (");
        if !is_cz && !is_zc {
            continue;
        }
        let open = match t.find('(') {
            Some(i) => i,
            None => continue,
        };
        let close = match t.rfind(')') {
            Some(i) => i,
            None => continue,
        };
        let args: Vec<&str> = t[open + 1..close].split(',').map(|s| s.trim()).collect();
        if args.len() < 2 {
            continue;
        }
        let id = match parse_id_or_symbol(args[0], &syms) {
            Some(id) => id,
            None => continue,
        };
        let len = match parse_len(args[1]) {
            Some(-1) => RefLen::Var,
            Some(l) => RefLen::Fixed(l),
            None => RefLen::Unresolved,
        };
        // last-wins within this source
        map.insert(
            id,
            RefEntry {
                is_cz,
                len,
                src: "ra:clif_packetdb.hpp",
            },
        );
    }
    Some(map)
}

fn strip_comment(line: &str) -> String {
    let mut s = line.to_string();
    if let Some(i) = s.find("//") {
        s.truncate(i);
    }
    if let Some(i) = s.find("/*") {
        s.truncate(i);
    }
    s
}

fn parse_int(s: &str) -> Option<u32> {
    let s = s.trim();
    if let Some(h) = s.strip_prefix("0x").or_else(|| s.strip_prefix("0X")) {
        u32::from_str_radix(h, 16).ok()
    } else {
        s.parse::<u32>().ok()
    }
}

fn parse_id_or_symbol(s: &str, syms: &BTreeMap<String, u16>) -> Option<u16> {
    let s = s.trim();
    if s.starts_with("0x") || s.starts_with("0X") {
        parse_int(s).map(|v| v as u16)
    } else if s.chars().next().map(|c| c.is_ascii_digit()).unwrap_or(false) {
        parse_int(s).map(|v| v as u16)
    } else {
        syms.get(s).copied()
    }
}

fn parse_len(s: &str) -> Option<i32> {
    let s = s.trim();
    if s.starts_with("sizeof") {
        return None;
    }
    if let Some(h) = s.strip_prefix("0x").or_else(|| s.strip_prefix("0X")) {
        return i32::from_str_radix(h, 16).ok();
    }
    s.parse::<i32>().ok()
}

fn classify(id_opt: Option<u16>, our_len: usize, refmap: &BTreeMap<u16, RefEntry>) -> &'static str {
    let id = match id_opt {
        None => return "NO-ID",
        Some(id) => id,
    };
    match refmap.get(&id) {
        None => "NO-REF",
        Some(e) if e.is_cz => "ID-REUSE",
        Some(e) => match e.len {
            RefLen::Var => "VAR",
            RefLen::Unresolved => "AMBIG",
            RefLen::Fixed(l) => {
                if l as usize == our_len {
                    "OK"
                } else {
                    "MISMATCH"
                }
            }
        },
    }
}

fn expected_str(id_opt: Option<u16>, refmap: &BTreeMap<u16, RefEntry>) -> (String, &'static str) {
    match id_opt.and_then(|id| refmap.get(&id)) {
        None => ("-".into(), "-"),
        Some(e) => {
            let s = match e.len {
                RefLen::Fixed(l) if e.is_cz => format!("cz({})", l),
                RefLen::Fixed(l) => l.to_string(),
                RefLen::Var => "-1".into(),
                RefLen::Unresolved => "?".into(),
            };
            (s, e.src)
        }
    }
}

fn handler_zc_names() -> Vec<String> {
    let text = std::fs::read_to_string(manifest_dir().join("src/handler.rs")).unwrap();
    let mut out = std::collections::BTreeSet::new();
    let bytes = text.as_bytes();
    let needle = b"PacketZc";
    let mut i = 0;
    while i + needle.len() <= bytes.len() {
        if &bytes[i..i + needle.len()] == needle {
            let start = i;
            let mut j = i + needle.len();
            while j < bytes.len()
                && (bytes[j].is_ascii_alphanumeric() || bytes[j] == b'_')
            {
                j += 1;
            }
            out.insert(String::from_utf8_lossy(&bytes[start..j]).to_string());
            i = j;
        } else {
            i += 1;
        }
    }
    out.into_iter().collect()
}

#[test]
fn zc_coverage_audit_drift() {
    // Every ZC name consumed by handler.rs must have a probe arm here.
    let consumed = handler_zc_names();
    std::panic::set_hook(Box::new(|_| {}));
    let missing: Vec<_> = consumed
        .iter()
        .filter(|n| {
            let n = (*n).clone();
            matches!(std::panic::catch_unwind(move || zc_probe(&n, 20120307)), Ok(None))
        })
        .cloned()
        .collect();
    let _ = std::panic::take_hook();
    assert!(
        missing.is_empty(),
        "handler.rs consumes ZC packets not in the audit probe (regenerate zc_coverage_audit.rs): {:?}",
        missing
    );
    assert_eq!(
        consumed.len(),
        CONSUMED_ZC.len(),
        "consumed ZC count drifted from committed list"
    );
}

#[test]
#[ignore = "prints the Step-0 coverage table; run with --ignored --nocapture"]
fn zc_coverage_audit() {
    for &ver in TARGETS {
        let refmap = match build_reference(ver) {
            Some(m) => m,
            None => {
                eprintln!("SKIP {ver}: reference sources (rathena/Hercules) not found");
                continue;
            }
        };
        struct Row {
            name: String,
            id: Option<u16>,
            exp: String,
            src: &'static str,
            our: usize,
            status: &'static str,
        }
        std::panic::set_hook(Box::new(|_| {}));
        let mut rows: Vec<Row> = Vec::new();
        for name in CONSUMED_ZC {
            let probed = std::panic::catch_unwind(|| zc_probe(name, ver).unwrap());
            let (id_opt, our_len, status) = match probed {
                Ok((id_opt, our_len)) => (id_opt, our_len, classify(id_opt, our_len, &refmap)),
                Err(_) => (None, 0usize, "PANIC"),
            };
            let (exp, src) = expected_str(id_opt, &refmap);
            rows.push(Row { name: name.to_string(), id: id_opt, exp, src, our: our_len, status });
        }
        let _ = std::panic::take_hook();
        rows.sort_by_key(|r| (r.id.unwrap_or(0xffff), r.name.clone()));

        use std::collections::BTreeMap as M;
        let mut counts: M<&'static str, usize> = M::new();
        for r in &rows {
            *counts.entry(r.status).or_insert(0) += 1;
        }

        println!("\n================ ZC COVERAGE AUDIT @ packetver {ver} ================");
        println!("{:<7} {:<44} {:>8} {:>9} {:<11} {}", "id", "struct", "exp_len", "base_len", "status", "src");
        for r in &rows {
            if r.status == "OK" {
                continue; // print only the worklist + notable statuses
            }
            let ids = r.id.map(|i| format!("0x{:04x}", i)).unwrap_or("-".into());
            println!("{:<7} {:<44} {:>8} {:>9} {:<11} {}", ids, r.name, r.exp, r.our, r.status, r.src);
        }
        println!("---- summary @ {ver}: {} consumed ----", rows.len());
        for (k, v) in &counts {
            println!("  {:<15} {}", k, v);
        }

        // cross-source disagreements: same id, different resolved fixed length.
        // first-source-wins in the merge above hides these; surface them here.
        let srcs = [
            parse_struct_source("../../../rathena/src/map/packets.hpp", ver, "ra:packets.hpp"),
            parse_struct_source("../../../Hercules/src/map/packets_struct.h", ver, "he:packets_struct.h"),
            parse_clif_packetdb(ver),
        ];
        let consumed_ids: std::collections::BTreeSet<u16> =
            rows.iter().filter_map(|r| r.id).collect();
        println!("---- cross-source length disagreements @ {ver} ----");
        for id in consumed_ids {
            let mut seen: Vec<(&'static str, i32)> = Vec::new();
            for s in srcs.iter().flatten() {
                if let Some(e) = s.get(&id) {
                    if !e.is_cz {
                        if let RefLen::Fixed(l) = e.len {
                            seen.push((e.src, l));
                        }
                    }
                }
            }
            let distinct: std::collections::BTreeSet<i32> =
                seen.iter().map(|(_, l)| *l).collect();
            if distinct.len() > 1 {
                let detail: Vec<String> =
                    seen.iter().map(|(s, l)| format!("{}={}", s, l)).collect();
                println!("  0x{:04x}  {}", id, detail.join("  "));
            }
        }
    }
}

// ================= inverse dispatch-coverage audit =================
// For each ZC packet the client consumes, take the id the SERVER actually emits
// at packetver V (from packets.hpp / packets_struct.h DEFINE ladders) and ask the
// REAL compiled parser what it does with it: dropped (PacketUnknown), or dispatched
// to some struct (and whether that struct's family matches). This is the true
// receive-side MISSING-LADDER check the per-struct length audit cannot express.

fn camel_to_snake_upper(s: &str) -> String {
    let ch: Vec<char> = s.chars().collect();
    let mut out = String::new();
    for (i, &c) in ch.iter().enumerate() {
        if i > 0 && c.is_ascii_uppercase() {
            let prev = ch[i - 1];
            if prev.is_ascii_lowercase() || prev.is_ascii_digit() {
                out.push('_');
            }
        }
        out.push(c.to_ascii_uppercase());
    }
    out
}

// "PacketZcItemPickupAck3" -> "ZC_ITEM_PICKUP_ACK" (strip) / "ZC_ITEM_PICKUP_ACK3"
fn to_family(pascal: &str, strip_trailing_digits: bool) -> String {
    let mut body = pascal.strip_prefix("PacketZc").unwrap_or(pascal).to_string();
    if strip_trailing_digits {
        while body.chars().last().map(|c| c.is_ascii_digit()).unwrap_or(false) {
            body.pop();
        }
    }
    format!("ZC_{}", camel_to_snake_upper(&body))
}

fn strip_trailing_digits(s: &str) -> String {
    let mut s = s.to_string();
    while s.chars().last().map(|c| c.is_ascii_digit()).unwrap_or(false) {
        s.pop();
    }
    s
}

fn consumed_family_set() -> std::collections::BTreeSet<String> {
    let mut set = std::collections::BTreeSet::new();
    for n in CONSUMED_ZC {
        set.insert(to_family(n, false));
        set.insert(to_family(n, true));
    }
    set
}

// active DEFINE (NAME, id) pairs at V from a struct source
fn struct_source_defs(rel: &str, target: u32) -> Vec<(String, u16)> {
    let text = match std::fs::read_to_string(manifest_dir().join(rel)) {
        Ok(t) => t,
        Err(_) => return Vec::new(),
    };
    let mut pp = Pp::new();
    let mut out = Vec::new();
    for raw in text.lines() {
        let line = strip_comment(raw);
        if pp.handle(&line, target) {
            continue;
        }
        if !pp.active() {
            continue;
        }
        if let Some(d) = parse_define(line.trim()) {
            out.push(d);
        }
    }
    out
}

// consumed ZC family NAME -> set of server-active ids at V
fn server_zc_defs(target: u32) -> BTreeMap<String, std::collections::BTreeSet<u16>> {
    let mut map: BTreeMap<String, std::collections::BTreeSet<u16>> = BTreeMap::new();
    for rel in [
        "../../../rathena/src/map/packets.hpp",
        "../../../Hercules/src/map/packets_struct.h",
    ] {
        for (name, id) in struct_source_defs(rel, target) {
            if name.starts_with("ZC_") {
                map.entry(name).or_default().insert(id);
            }
        }
    }
    // entry/itemlist families carry no DEFINE_PACKET_HEADER; their id-per-version
    // lives in the packets_struct.hpp symbolic enums.
    let syms = symbol_ids(target);
    for (symbol, family) in [
        ("idle_unitType", "ZC_NOTIFY_STANDENTRY"),
        ("spawn_unitType", "ZC_NOTIFY_NEWENTRY"),
        ("unit_walkingType", "ZC_NOTIFY_MOVEENTRY"),
        ("inventorylistnormalType", "ZC_NORMAL_ITEMLIST"),
        ("inventorylistequipType", "ZC_EQUIPMENT_ITEMLIST"),
        ("storageListNormalType", "ZC_STORE_NORMAL_ITEMLIST"),
        ("storageListEquipType", "ZC_STORE_EQUIPMENT_ITEMLIST"),
        ("cartlistnormalType", "ZC_CART_NORMAL_ITEMLIST"),
        ("cartlistequipType", "ZC_CART_EQUIPMENT_ITEMLIST"),
        // reliable only because clif.cpp sends these via `p.PacketType = <symbol>`
        // (verified send-path truth); enum symbols with a hardcoded header switch
        // in clif.cpp (skill_entry/status_change/quest*/equipitem) are NOT added --
        // their enum id can diverge from what the send path actually emits.
        ("dropflooritemType", "ZC_ITEM_FALL_ENTRY"),
        ("sendLookType", "ZC_SPRITE_CHANGE"),
        ("useItemAckType", "ZC_USE_ITEM_ACK"),
    ] {
        if let Some(&id) = syms.get(symbol) {
            map.entry(family.to_string()).or_default().insert(id);
        }
    }
    map
}

// what the real compiled parser does with `id` at `v`
enum Dispatch {
    Unknown,
    Struct(String),
}

fn parser_dispatch(id: u16, v: u32) -> Dispatch {
    let mut buf = vec![0u8; 2048];
    buf[0] = (id & 0xff) as u8;
    buf[1] = (id >> 8) as u8;
    let res = std::panic::catch_unwind(|| {
        let p = packets::packets_parser::parse(&buf, v);
        let unknown = p.as_any().downcast_ref::<PacketUnknown>().is_some();
        (unknown, p.name().to_string())
    });
    match res {
        Ok((true, _)) => Dispatch::Unknown,
        Ok((false, name)) => Dispatch::Struct(name),
        // an arm matched but from() panicked on the zeroed stub buffer => covered
        Err(_) => Dispatch::Struct("<matched>".into()),
    }
}

#[test]
#[ignore = "prints the inverse dispatch-coverage table; run with --ignored --nocapture"]
fn zc_dispatch_coverage() {
    let fams = consumed_family_set();
    for &ver in TARGETS {
        let defs = server_zc_defs(ver);
        if defs.is_empty() {
            eprintln!("SKIP {ver}: reference sources not found");
            continue;
        }
        std::panic::set_hook(Box::new(|_| {}));
        let mut dropped: Vec<(String, u16)> = Vec::new();
        let mut misdispatched: Vec<(String, u16, String)> = Vec::new();
        let mut checked = 0usize;
        let mut covered = 0usize;
        for (name, ids) in &defs {
            let consumed =
                fams.contains(name) || fams.contains(&strip_trailing_digits(name));
            if !consumed {
                continue;
            }
            for &id in ids {
                checked += 1;
                match parser_dispatch(id, ver) {
                    Dispatch::Unknown => dropped.push((name.clone(), id)),
                    Dispatch::Struct(got) => {
                        covered += 1;
                        if got != "<matched>" {
                            let got_fam = to_family(&got, true);
                            let want_fam = strip_trailing_digits(name);
                            if got_fam != want_fam {
                                misdispatched.push((name.clone(), id, got));
                            }
                        }
                    }
                }
            }
        }
        let _ = std::panic::take_hook();

        // consumed families with no DEFINE in the reference sources -> the inverse
        // audit cannot verify these (clif_packetdb-only / unnamed); report honestly.
        let mut unchecked: Vec<String> = Vec::new();
        for n in CONSUMED_ZC {
            let fam = to_family(n, true);
            if !defs.contains_key(&fam) && !defs.keys().any(|k| strip_trailing_digits(k) == fam) {
                if !unchecked.contains(&fam) {
                    unchecked.push(fam);
                }
            }
        }
        unchecked.sort();

        dropped.sort();
        misdispatched.sort();
        println!("\n========== ZC DISPATCH COVERAGE @ packetver {ver} ==========");
        println!("consumed families checked (server-active ids): {checked}");
        println!("consumed families NOT in reference DEFINEs (unverifiable here): {}", unchecked.len());
        for u in &unchecked { println!("    UNVERIFIABLE {}", u); }
        println!("covered by parser: {covered}   DROPPED: {}   MISDISPATCH: {}", dropped.len(), misdispatched.len());
        if !dropped.is_empty() {
            println!("-- DROPPED (server emits, parser returns PacketUnknown) --");
            for (name, id) in &dropped {
                println!("  0x{:04x}  {}", id, name);
            }
        }
        if !misdispatched.is_empty() {
            println!("-- MISDISPATCH (parser routes server id to wrong family) --");
            for (name, id, got) in &misdispatched {
                println!("  0x{:04x}  server={:<32} parser->{}", id, name, got);
            }
        }
    }
}
